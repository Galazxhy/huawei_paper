# 论文框架（优化版，待确认）

> 目标：正文约 60 页，四个大问题主线清楚；每个问题用“准备 → 计算 → 结果 → 边界”推进，
> 标题用**输出对象或动作**命名，不统一挂“模型”后缀；中间表、参数网格、完整矩阵和代码放附录。

## 一、全文框架与篇幅预算

| 章节 | 建议页数 | 说明 |
| --- | ---: | --- |
| 摘要 | 1 | 四问各一段，突出方法、结果、边界 |
| 1 问题重述 | 2 | 1.1 背景；1.2 问题提出 |
| 2 基本假设与符号说明 | 1.5 | 假设 6～8 条；符号表只列核心符号 |
| 3 数据说明与预处理 | 3 | 数据清单、指标预处理、领域不平衡、配比数据准备 |
| 4 问题一：质量、冲突、配比 | 16 | 见下 |
| 5 问题二：广义标度律 | 12 | B1 主拟合、B2/B3 验证、B4/B5 对照、B6–B8 质量 |
| 6 问题三：算力约束资源优化 | 13 | C7、三档预算、结构转移、敏感性 |
| 7 问题四：技术演进与前沿预测 | 9 | C1/C2/C3/C4/C8、Loss–Benchmark 桥接 |
| 8 模型评价与推广 | 1.5 | 优点、局限、推广 |
| 参考文献 | 1 | 统一编号 |
| 附录 | 8～12 | 代码、完整审计表、完整矩阵、敏感性网格 |

正文控制在 58～60 页；附录不计入正文阅读负担。

## 二、问题一框架

问题一对应赛题三个子任务，标题直接采用赛题原词，不统一加“模型”：

| 小节 | 对应赛题任务 | 最终输出 | 关键创新 |
| --- | --- | --- | --- |
| 4.1 数据质量评价 | 统一方向、建立综合评价、给样本/语料/领域级 Q、A1+A2/A3 全量、域级对照 | `Q^(0)` | 域均衡参考尺度、信息簇去冗余、方向审计 |
| 4.2 质量冲突消解 | 定义冲突、分析成因、建立综合评价、扩展集检验 | `Q*`、`U`、`C` | 域感知冲突、数据驱动显著性、可靠性重加权 |
| 4.3 领域配比—Loss 关系 | p 与 Loss 的定量关系、单域及组合作用、质量引入论证、A6–A11 检验、A12–A15 外推 | `L(p)`、`phi(p)`、`S`、`C_ij` | 多任务稀疏混合响应、替换/互补结构、外推失效边界 |

章首只放两件事：一段文字说明 `22维信号 -> Q^(0) -> Q* -> Q17 -> p -> L(p)`；一张技术路线图。

### 4.1 数据质量评价（5 页）

故事链：审计指标 → 统一方向 → 域均衡尺度 → 信息簇合成 → 结果检验。
本小问产出是统一质量分 `Q^(0)`，不是预测模型，标题因此不写“模型”。

#### 4.1.1 指标标量化、方向统一

- 22 个字段的类型、缺失、分布、方向压成一张总表。
- 中心适宜只保留 5 个指标：`word_count, num_sentences, unigram_entropy, uppercase,
  mean_word_length`；其余统一正向/负向。
- 只报一行方向证据：新方案 7 域排序 Spearman=1，AI 原文评审相关 0.666；全正向仅 0.429。
- 图：22 指标相关聚类热力图。

#### 4.1.2 域均衡尺度

- 只保留 `F_j^bal` 和适宜度 `s_ij` 两个公式。
- 说明为什么不用全样本 Min-Max、为什么七域各自归一化会丢失跨域可比性。

#### 4.1.3 信息簇合成

- 保留 `1-|rho|` 聚类、`K=9`、`H_ik`、`Q^(0)` 一条链。
- 强调“簇内融合、簇间等权”避免冗余指标重复计票。

#### 4.1.4 结果检验

- 图/表：7 域 `Q^(0)` + 95%CI。
- 图：A2/A3 扩展对照；只写 arxiv/github 差值 -0.0017 和 -0.0001。
- 原文核验只写 AI 辅助结论和代码域偏差风险；210 条完整案例表放附录。

### 4.2 质量冲突消解（4.5 页）

故事链：定义冲突 → 识别冲突 → 修正证据 → 验证成因。
本小问不追求“平均修正很大”，而是证明修正集中在高冲突尾部。

#### 4.2.1 冲突定义

- 明确“指标差异不等于冲突，冲突是偏离领域正常关系”。
- 放质量关系图 `G=(V,E)`，135 条边压成一张网络图。

#### 4.2.2 冲突识别

- 保留 cross-fitting 残差、`r_ij`、`p_conf`、`C_i` 四个公式。
- 说明 5 折 cross-fitting 避免自己预测自己。

#### 4.2.3 证据可靠性重加权

- 保留 `R_ij -> 簇可靠度 -> 自适应权重 -> Q*` 一条链。
- 输出三元组 `(Q*, U, C)`；只给七域最终质量表。
- 强调冲突降低不可靠证据权重，而不是直接扣分。

#### 4.2.4 成因验证

- 表：Top 1%/5%/10% 的 `|Q*-Q0|` 和 64.8% 修正贡献。
- 正文放 3 个典型高冲突案例；其余 11 个和完整关系矩阵放附录。

### 4.3 领域配比—Loss 关系（5.5 页）

故事链：数据准备 → 多任务响应 → 替换效应 → 互补结构 → 迁移边界。
这是问题一中唯一真正建立映射关系的部分，标题直接写“配比—Loss 关系”。

#### 4.3.1 配比、Loss 的数据结构

- 13 域 Loss 标准化只保留 `z = (L - median)/(1.4826 MAD)`。
- A16 的 7 域—17 域映射只用一张表。
- 质量修正配比 `p_tilde(gamma)` 在此构造，`gamma=0` 结论放这里。

#### 4.3.2 多任务混合响应

- 二阶 Scheffé 公式、153 维特征、Group Elastic Net。
- 表：一阶 Scheffé、Scheffé、ILR-Ridge 的 1M 检验性能；明确“ILR 预测、Scheffé 解释”。

#### 4.3.3 领域替换结构

- 图：17×17 替换矩阵。
- 只解释通过覆盖、符号稳定、效应量门槛的转移方向。

#### 4.3.4 领域互补结构

- 图：互补网络。
- 说明替换与互补是两个不同机制，不混写。

#### 4.3.5 跨尺度迁移边界

- 图/表：1M/60M/1B 排序一致性，10B/70B 外推失效边界。
- 结论：真实 1M–1B 内配比方向可迁移；10B/70B 外推数据不支持尺度不变假设。

### 问题一正文图表压缩

正文只保留 7 张图和 5 张表：

| 图 | 位置 |
| --- | --- |
| 问题一技术路线图 | 章首 |
| 22 指标相关聚类图 | 4.1.1 |
| 7 域 Q0+CI、A2/A3 扩展对照 | 4.1.4 |
| 质量关系图、冲突尾部修正 | 4.2 |
| 替换矩阵、互补网络 | 4.3.3、4.3.4 |
| 跨尺度一致性图 | 4.3.5 |
| 外推误差-距离图 | 4.3.5 |

| 表 | 位置 |
| --- | --- |
| 22 指标方向审计摘要 | 4.1.1 |
| 七域最终质量 `Q*` | 4.2.3 |
| 冲突尾部修正证据 | 4.2.4 |
| 模型预测性能对比 | 4.3.2 |
| 跨尺度检验汇总 | 4.3.5 |

### 放进附录的内容

- 22 指标完整审计表、9 个信息簇完整权重。
- 210 条 AI 辅助原文评审表、14 个高冲突案例完整表。
- 17×17 替换/互补矩阵完整数值及 bootstrap 置信区间。
- 方向方案、alpha/lambda、cluster K 的全部敏感性网格。
- 全部源代码。

## 三、后三问的框架接口

### 5 问题二：跨维度数据融合与广义标度律

- 5.1 问题二分析
- 5.2 B1–B5 数据说明、广义标度律形式
- 5.3 含 `Q`、`p` 的广义标度律
- 5.4 B2/B3 轨迹验证、B4/B5 跨族对照
- 5.5 B6–B8 质量独立效应
- 5.6 弹性与替代条件

### 6 问题三：算力约束下的资源联合优化

- 6.1 问题三分析与算力结构
- 6.2 训练、质量提升、长上下文三类成本
- 6.3 算力约束优化模型
- 6.4 三档预算求解、结构转移
- 6.5 成本函数形式比较与敏感性

### 7 问题四：技术演进分析与前沿预测

- 7.1 问题四分析与开源筛选口径
- 7.2 规模扩张、非规模技术进步的分解
- 7.3 Loss–Benchmark 桥接
- 7.4 未来 12/24 个月前沿预测
- 7.5 不确定性与情景分析

## 四、写作压缩原则

1. 每个模型只保留“为什么需要—怎么定义—结果如何—边界在哪”四句话的逻辑链。
2. 一个结论只配一张图或一张表，不在正文重复列数值。
3. 所有完整矩阵、全部参数网格、完整案例表、代码一律进附录。
4. 问题一正文不超过 16 页；三级标题数量随逻辑需要取 3～5 个，不强行对齐。
5. 先放图/表占位，再补文字；文字只解释图表，不复述图表。
