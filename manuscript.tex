% !Mode:: "TeX:UTF-8"
%!TEX program = xelatex

%\documentclass[bwprint,fontset=windows]{gmcmthesis}
\documentclass[bwprint]{gmcmthesis}

\hypersetup{hidelinks}
\usepackage{subcaption}
\usepackage{amsmath}
\usepackage{siunitx}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{caption}
\usepackage{placeins}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,shapes.geometric}
\captionsetup[figure]{justification=centering,font=small,labelsep=quad}
\captionsetup[table]{justification=centering,font=small,labelsep=quad}

% 算法
\usepackage[noend]{algpseudocode}
\usepackage{algorithmicx,algorithm}
\floatname{algorithm}{算法}
\renewcommand{\algorithmicrequire}{\textbf{输入:}}
\renewcommand{\algorithmicensure}{\textbf{输出:}}

% 章节编号
\renewcommand\thesection{\chinese{section}、}
\renewcommand\thesubsection{\arabic{section}.\arabic{subsection}}
\renewcommand\thesubsubsection{\thesubsection.\arabic{subsubsection}}

% 统一表格格式：三线表；描述列左对齐，数值列居中；紧凑但不拥挤
\newcolumntype{Y}{>{\centering\arraybackslash}X}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\newcolumntype{C}[1]{>{\centering\arraybackslash}m{#1}}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}m{#1}}
\newcommand{\TableSetup}{%
    \centering
    \small
    \setlength{\tabcolsep}{5pt}%
    \renewcommand{\arraystretch}{1.13}%
}
\newcommand{\CompactTableSetup}{%
    \centering
    \footnotesize
    \setlength{\tabcolsep}{4pt}%
    \renewcommand{\arraystretch}{1.08}%
}
\newcommand{\TableNote}[2][0.96\textwidth]{%
    \vspace{0.25em}\par
    \begin{minipage}{#1}
    \small\raggedright #2
    \end{minipage}
}

%===================== 建模论文题目 ======================
\title{全国研究生数学建模竞赛论文标题}
\baominghao{\hspace{6em} 20250900001}
\schoolname{\hspace{5.8em} 中南大学}
\membera{\hspace{6em} 彭铸}
\memberb{\hspace{6em} 阳挺}
\memberc{\hspace{6em} 郑徐瀚宇}
%=======================================================

\begin{document}

\maketitle

\begin{abstract}
    算力与高质量语料的双重约束，使大语言模型的性能提升不再只是参数量和训练 Token 数的扩张问题。数据的质量与领域组成影响单位 Token 的训练价值，而质量治理和长上下文计算又会占用有限预算。本文围绕语料评价、广义资源--损失关系及联合配置建立递进模型，以验证损失衡量前三问的性能响应，并区分真实观测、半合成校准与外推情景的证据边界。

    \textbf{针对问题一：}对七个语料领域的 22 项异质质量信号进行优劣方向审计、域均衡标度和九个信息簇的层次聚合，形成样本与领域质量分；再以域内条件残差识别异常冲突，并对相应证据作可靠性重加权。同源扩展语料与对应抽样领域的质量均值接近；质量评分与 210 条人工复核结果的 Spearman 相关为 $0.666$。冲突修正前后的全样本排序相关为 $0.9992$，而高冲突的前 $10\%$ 样本贡献了 $64.8\%$ 的总绝对修正量，表明校正主要作用于异常尾部。针对 17 域训练配比，在单纯形约束下分别以 ILR--Ridge 预测损失、以多任务稀疏 Scheff\'e 模型解释来源替换与交互；前者在真实检验集的 13 个评估目标上取得平均 $R^2=0.765$。配比排序相关在 60M 和 1B 规模分别为 $0.810$ 与 $0.608$，据此界定配比规律的规模迁移边界。

    \textbf{针对问题二：}以真实训练轨迹锚定经典标度律，对异源损失实施仿射尺度归一化，并以可分离非线性最小二乘和自适应结构早停辨识规模参数。跨模型族观测的归一化 $R^2$ 为 $0.9240$，说明规模规律在校准损失口径后具有一定迁移性。在经典基线上，以质量加权来源份额和中心化对数比特征刻画配比响应，采用锚定基线、分阶段辨识及因子对照的方式建立广义标度律；13 个评估目标在三个已观测规模上的留出预测相对 RMSE 汇总为 $5.54\%$，中心化配比效应的 $R^2$ 为 $0.9006$。单独变化质量的校准数据主拟合 $R^2$ 为 $0.9568$，在 $Q=0.6$ 处的损失弹性约为 $-0.0482$。固定参考配比、$N=10^9$ 及 $D=250$ 亿 Token 时，质量由 $0.6$ 提升至 $0.7$ 的等损失 Token 节省量为 $13.97$ 亿；叠加可行的一个百分点配比调整后，中位数为 $14.06$ 亿。上述数值是质量--配比耦合假设下的模型情景推算，其适用边界由观测规模、质量标定范围及配比支撑域共同限定，超大规模质量响应与实际训练收益尚不在验证范围内。

    \textbf{针对问题三：}以前两问交接的逐目标损失响应构造优化目标，将配比限制在 512 组真实训练配方的凸包内，联合配置参数量、Token 数、领域份额与治理质量；成本同时计入基础训练、增量质量治理和外生上下文长度对应的注意力开销。在 11 档预算、5 档上下文及 3 种质量成本函数形成的 165 个联合配置情景中，相对同预算规模基线的平均损失改善为 $4.16\%$--$5.81\%$。其中 162 个情景至少有一个评估目标的损失上升，明确了平均改善的逐领域收益边界；有限候选在已设定质量收益情景中的最大相对后悔值为 $1.5465\%$。注意力成本与基础训练成本相当的临界上下文长度为 $30000$，超过该长度后序列开销显著挤占规模预算。固定配比的局部情景出现质量治理启动，但联合配置未达到跨情景稳定的结构转移判据。配置数值限定于已辨识响应函数和既定成本口径，实际应用仍需重新校准治理成本及逐领域损失。

    \keywords{数据质量评价；领域配比；广义标度律；质量--规模替代；算力约束优化}
\end{abstract}

\pagestyle{plain}
\maketoc
\clearpage

% =====================================================
% 一、问题重述
% =====================================================
\section{问题重述}
\subsection{问题背景}

大语言模型正逐渐成为知识获取、内容生成与复杂任务处理的基础技术。其能力提升长期依赖更大的参数规模和更充分的训练，但参数、数据与算力的扩张需要同步投入：模型越大，单次训练所需的计算和存储资源越多；训练越久，新增 Token 带来的收益也逐步减弱。规模投入仍然重要，却难以单独回答有限资源应投向何处的问题。

训练语料并非同质的 Token 集合。不同来源在事实密度、语言表达、专业知识、代码与数学内容以及重复和噪声程度上存在差异；数量相同的语料，对模型能力的贡献可能并不相同。高质量语料的获取、清洗和组织需要额外投入，某些专业领域的有效内容又难以无限扩充。与此同时，训练数据由多个领域共同组成，一个领域份额的增加必然挤占其他领域，因而语料配比体现的是不同知识覆盖之间的取舍，而不是可以独立增加的资源总量。

有限算力进一步耦合了这些选择。扩大模型、增加训练 Token、提升语料质量与支持更长上下文都会消耗预算；长序列带来的注意力开销尤其会改变既定预算中可用于参数和数据的比例。不同投入的收益和成本随资源水平而变化，低预算下的有效策略未必适用于更高预算。另一方面，架构、训练方法和数据工程的进步也会改变资源转化为模型能力的效率，使能力增长不能完全归因于规模扩张。如何协调模型规模、语料数量与价值、领域结构及上下文需求，因而成为算力约束下大语言模型发展的核心资源配置问题。

\subsection{问题重述}
本文围绕“有限算力如何转化为更高模型能力”这一核心问题，依次研究数据价值、训练损失、资源配置和能力前沿。四项任务既相互衔接，又对应不同的观测量和验证方式：
\begin{enumerate}
    \item \textbf{数据质量评价与领域配比建模。}多维质量信号的量纲、优劣方向和领域覆盖各不相同，首先需要将其转化为方向一致、可跨领域比较的质量评分，并明确由样本到语料及训练来源的聚合规则。当同一文本的质量指标相互矛盾时，还需定义冲突程度、分析证据可靠性并提出消解方法，检验结论在不同数据范围内是否稳定。在此基础上，针对训练来源份额非负且总和为 $1$ 的约束，建立领域配比与验证损失的定量关系，辨别单一来源的替换效应和来源组合的互补或竞争作用。
    \item \textbf{广义标度律与替代条件。}不同训练轨迹及模型体系的原始损失未必处于统一尺度，需要先建立可比口径，辨识经典标度律并检验其跨轨迹、跨模型族的泛化能力。随后将整体数据质量 $Q$ 和领域配比 $\mathbf p$ 纳入 $N$、$D$ 与 $L$ 的关系，提出可估计、可验证的广义标度律；其中质量效应与配比效应必须区分，不能从固定来源质量的配比变化中直接推断独立的质量收益。进一步计算各因素的边际降损与弹性，在给定工作点和目标损失下，推导语料质量、配比与规模投入可相互替代的条件及其外推边界。
    \item \textbf{算力预算下的资源联合优化。}在训练开销、质量提升开销和长上下文开销共同约束下，求解参数量、训练 Token 数、整体质量与领域配比的联合配置。上下文长度由架构与任务需求外生给定，而非与其他因素一同自由寻优；需分析其可行取值对剩余预算的挤占。比较不同质量成本形式及多个量级的算力预算，给出最优配置和性能变化，并用可检验的判据识别预算增加时资源分配是否出现结构性转移。
    \item \textbf{技术演进分解与能力前沿预测。}利用历史模型的发布日期、规模与多维评测记录，界定综合能力指标以及开源模型、预训练模型和对话微调模型的比较口径，分离规模扩张与非规模技术进步对能力增长的贡献。由于前三项任务以验证损失为主要结果变量，而能力前沿以评测得分衡量，还需建立两者之间的桥接关系并分析映射误差。在算力增长放缓的情景下预测未来开源模型的能力边界，同时给出时间跨度、任务差异和数据不确定性对预测的影响。
\end{enumerate}

% =====================================================
% 二、符号说明
% =====================================================
\section{符号说明}
 {\small
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.08}
  \setlength{\LTleft}{\fill}
  \setlength{\LTright}{\fill}
  \captionof{table}{主要符号说明}\label{tab:notation}
  \centering
  \begin{longtable}{@{}C{2.7cm}P{8.3cm}C{2.1cm}@{}}
      \toprule
      符号                                            & 含义                                                                         & 使用章节  \\
      \midrule
      \endfirsthead
      \multicolumn{3}{r}{表~\ref{tab:notation}（续）}                                                                                        \\
      \toprule
      符号                                            & 含义                                                                         & 使用章节  \\
      \midrule
      \endhead
      \bottomrule
      \endlastfoot
      $L_{mk},L$                                    & 配方 $m$ 在评估目标 $k$ 上的验证损失；$L$ 为给定目标时的简写                                      & 第4、5节 \\
      $\mathbf p,p_i$                               & 训练来源配比向量及来源 $i$ 的份额，$\sum_i p_i=1$                                         & 第4、5节 \\
      $\mathbf Q^{(17)}$                            & 问题一由校正质量映射得到的 17 域来源质量向量                                                   & 第4、5节 \\
      \midrule
      $x_{ij},s_{ij}$                               & 样本 $i$ 的第 $j$ 项原始质量信号及方向统一后的表示                                             & 第4节   \\
      $F_j^{\mathrm{bal}}$                          & 第 $j$ 项质量信号的域均衡经验分布                                                        & 第4节   \\
      $\mathcal P,\mathcal N,\mathcal M$            & 正向、负向与中心适宜指标集合                                                             & 第4节   \\
      $G_k,a_{jk},K$                                & 信息簇、簇内指标权重及信息簇个数（$K=9$）                                                    & 第4节   \\
      $\eta_j$                                      & 第 $j$ 项信号的领域间方差占比                                                          & 第4节   \\
      $Q_i^{(0)},Q_i^*$                             & 样本 $i$ 的初始质量分数与冲突校正质量分数                                                    & 第4节   \\
      $r_{ij},I_{ij},C_i$                           & 标准化异常度、冲突指示变量及样本冲突度                                                        & 第4节   \\
      $R_{ij},U_i$                                  & 指标证据可靠度及样本的信息簇平均可靠度                                                        & 第4节   \\
      $z_{mk}$                                      & 按评估目标标准化后的验证损失                                                             & 第4节   \\
      $\phi(\mathbf p)$                             & 13 个评估目标的总体配比表现，越大越优                                                       & 第4节   \\
      $S_{i\leftarrow j},C_{ij}^{\mathrm{mix}}$     & 来源份额的边际替换效应及两来源互补指数                                                        & 第4节   \\
      $\rho_s$                                      & Spearman 等级相关系数；不作组成响应幅度使用                                                 & 第4节   \\
      \midrule
      $N,D,C$                                       & 模型参数量、训练 Token 数及总 FLOPs 预算；训练项为 $C_{\mathrm{train}}=6ND$                  & 第5、6节 \\
      $E,A,B$                                       & 不可约损失及参数项、数据项的幅度系数                                                         & 第5节   \\
      $\alpha,\beta$                                & 参数量与数据量的经典标度指数                                                             & 第5节   \\
      $a_j,b_j,\widetilde L$                        & 来源 $j$ 的损失仿射校准系数及归一化损失                                                     & 第5节   \\
      $\xi,\omega_j,J_m$                            & 验证难度指数、来源验证权重及候选结构评分                                                       & 第5节   \\
      $Q_i,\mathbf Q$                               & 通用来源质量及其向量；本文 17 域实例取 $\mathbf Q=\mathbf Q^{(17)}$                         & 第5节   \\
      $\varphi(\mathbf Q,\mathbf p)$                & 逐目标的组成损失修正；不同于 $\phi(\mathbf p)$                                           & 第5节   \\
      $w_i,w_{\mathrm{clr},i}$                      & 质量加权份额与相对配比特征的有效系数                                                         & 第5节   \\
      $\operatorname{clr}_{\varepsilon}(\mathbf p)$ & 加伪计数 $\varepsilon$ 的中心化对数比变换                                               & 第5节   \\
      $N_0,D_0$                                     & 广义律的首个规模归一化锚点                                                              & 第5节   \\
      $\eta,\zeta,M(N,D)$                           & 组成响应的规模指数及调制因子 $M=(N/N_0)^{-\eta}(D/D_0)^{-\zeta}$                         & 第5节   \\
      $\kappa_s,b_s$                                & 锚点 $s$ 的组成响应幅度及规模基线损失                                                      & 第5节   \\
      $Q,Q_0,Q_1$                                   & 整体质量等级及比较点 $Q_0=0.6$、$Q_1=0.7$；非样本质量 $Q_i^{(0)}$                           & 第5节   \\
      $\gamma_Q$                                    & 独立质量实验标定的整体质量指数                                                            & 第5节   \\
      $D_\star,\mathbf p_0$                         & 等损失替代的 Token 工作点及参考配比；$D_\star\ne D_0$                                     & 第5节   \\
      $g_x,\epsilon_x$                              & 因素 $x$ 的边际降损与损失弹性                                                          & 第5节   \\
      $L_{\mathrm{ctx}},\eta_{\mathrm{attn}}$       & 外生上下文长度及注意力成本系数；$C_{\mathrm{attn}}=\eta_{\mathrm{attn}}NDL_{\mathrm{ctx}}$ & 第6节   \\
      $Q_{\mathrm{base}},Q,h(Q)$                    & 第三问治理基线质量、治理决策质量及单位 Token 增量治理成本                                           & 第6节   \\
      $C_Q,C_{\mathrm{used}},a,s_Q$                 & 质量治理成本、实际总成本、合并系数 $a=6+\eta L_{\mathrm{ctx}}$ 及治理成本份额                      & 第6节   \\
  \end{longtable}
 }

% =====================================================
% 三、数据说明与预处理
% =====================================================
\section{数据说明与预处理}

\subsection{数据来源及可信度分层}

本文按证据功能而非文件顺序组织数据。语料质量评价以多维质量信号抽样及扩展记录（A1--A3）为基础；领域配比与验证损失由成对的训练表（A4、A5）建立关联，并以同尺度及跨尺度检验表（A6--A11）评估泛化。更大规模的配比与损失表（A12--A15）属于子集配方和外推估计，仅用于考察趋势的稳健性；跨领域质量映射采用参考对应关系（A16），其不确定性在解释时保留。

规模基线锚定于真实训练日志（B1），再以半合成的族外日志（B2）、插值轨迹（B3）、跨模型族观测（B4）及文献数据（B5）进行分层检验。独立质量变化由校准生成的质量实验（B6、B7）提供；含大规模推算的扩展表（B8）不作为直接观测验证。超大模型的参数资料与预估损失（B9、B10）也分别承担情景设定和外推对照的角色，不能混作同等级证据。能力演进分析可依托多维排行榜及其替代版本（C1、C2）、时序记录（C3）、模型元数据（C4）、损失--评测桥接样本（C5、C6）和逐任务结果（C8）；上下文长度的可行范围则参考架构元数据（C7）。上述来源在建模中分别用于估计、验证或情景分析，半合成、插值与估算结果均不等同于独立实测。

\subsection{数据口径统一}

质量信号先按指标含义统一为“越高越好”，再将各领域经验分布等权合成为公共标尺；由抽样语料确定的标量化和变换规则在同源扩展语料中保持不变。样本质量经领域聚合后，通过参考映射构成训练来源的质量描述 $\mathbf Q^{(17)}$。这一向量与后文表示整体治理等级的标量 $Q$ 含义不同：在配比记录中各来源 $Q_i^{(17)}$ 固定，$p_iQ_i^{(17)}$ 不能提供独立改变质量的信息。

配方表与损失表按共同标识逐行对应；17 个训练来源份额满足单纯形约束，13 个评估目标的损失保留各自口径。问题一的总体配比指标在训练样本内逐目标进行中位数--MAD 标准化。问题二以主训练日志的交叉熵损失为参考尺度，对其他模型来源只校准其仿射位置与幅度，不直接合并原始损失。参数量与 Token 数在计算成本中统一换算为绝对个数，以免十亿单位与 FLOPs 混用。

\subsection{训练、检验及外推数据划分}

配比响应由训练配方及对应损失估计，同尺度和跨尺度的真实检验表用于评价迁移；更大规模的估算损失仅用于情景外推。同源扩展质量语料用于检查抽样评价的代表性，不作为独立重复实验。经典标度律的参数由主训练日志估计，其他来源参与结构选择与分层比较，但不回流重估主律参数；因此结构选择所用数据与完全未接触的外部测试须加以区分。

广义律按配方划分拟合与留出样本：逐行配对的规模使用相同配方划分，另一观测规模独立划分，避免同一配方在训练与检验间泄漏。两组质量实验存在精确的样本包含关系，只能提供子集一致性对照；含大规模推算的质量表与预估损失不计作直接观测。问题三以此前辨识的损失响应进行条件优化，其预算情景结果也不等同于新增的模型训练实验。

% =====================================================
% 四、问题一
% =====================================================
\section{问题一：数据质量评价、冲突消解及领域配比}
\label{sec:q1}

附件 A 包含两类相互衔接的数据，A1--A3 给出语料的多维质量信号，A4--A15 给出不同领域配方及其验证损失。
本问依次解决三个层次的问题。首先在七域样本不均衡条件下构造初始质量 $Q^{(0)}$；随后依据各领域内部的正常指标关系识别异常评价证据，并校正其权重得到 $Q^*$；最后在单纯形约束下建立 17 域训练配比对 13 域验证 Loss 的联合响应。
$Q^*$ 经 A16 映射为来源质量向量 $\mathbf Q^{(17)}$，并与总体配比效应 $\phi(\mathbf p)$ 一同作为问题二的数据侧输入。

% -----------------------------------------------------
% 4.1 数据质量评价
% -----------------------------------------------------
\subsection{数据质量评价}
\label{subsec:q1_quality}

A1 含 51,230 条样本、7 个领域和 22 项质量信号；A2、A3 分别是 arxiv 和 github 的扩展抽样集合，各含 17,523 条和 203,752 条样本，并完整包含 A1 对应领域的 1,419 条与 10,000 条样本。
A2、A3 采用与 A1 相同的字段口径和采集来源，因此可用于检验 A1 的抽样代表性。
综合评价面临三项结构性困难，即七域样本量差异显著、多项评价信号存在重复信息，以及部分文本结构指标具有合理区间而非简单单调关系。
针对这三点，依次进行质量信号统一表征、域均衡标度、信息簇合成和人工一致性检验。

\subsubsection{质量信号统一表征与方向审计}

22 项信号包含 8 个列表字段和 14 个标量字段。列表字段按原始含义先行标量化，二分类 logits 经 softmax 转为目标类别概率，六级有序 logits 取期望等级并按 5 归一化，QuRating 的四个异质量纲分量先分别作百分位变换再取均值。
原始数据完整性良好，仅 \texttt{modernbert\_reasoning} 与 \texttt{modernbert\_professionalism} 分别出现 13 条和 5 条无效 logits，以所属领域的中位数填补，占总样本比例不足 $0.04\%$。

标量化只解决量纲不一致，并不解决方向不一致。为统一综合分数的含义，本文对 22 项信号逐一登记方向，分为正向、负向和中心适宜三类，最终仅 WordLength、Sentences、WordCount、Entropy 与 Uppercase 设为中心适宜，其余按单调方向处理；方向设定的依据与消融见 \ref{subsubsec:q1_consistency} 节。
表~\ref{tab:q1_indicator} 汇总 22 项信号的短名、含义、字段形态、标量化方式以及统一表示 $s_{ij}$ 的分布特征，信息簇归属与方向判定见图~\ref{fig:q1_quality_structure}。

\begin{table}[htbp]
    \centering
    \caption{22 项质量信号的统一表征与分布特征}
    \label{tab:q1_indicator}
    \scriptsize
    \setlength{\tabcolsep}{4pt}
    \renewcommand{\arraystretch}{1.06}
    \begin{tabular}{@{}C{1.80cm} C{2.10cm} C{1.35cm} C{2.55cm} C{1.10cm} C{1.20cm} C{1.20cm}@{}}
        \toprule
        信号短名        & 含义      & 字段形态 & 标量化方式                                & $\bar{s}$ & $\sigma_{s}$ & $\eta_j$ \\
        \midrule
        NoAlpha     & 非字母词比例  & 标量   & 直接使用                                 & 0.516     & 0.310        & 0.49     \\
        Uppercase   & 大写字母比例  & 标量   & 直接使用                                 & 0.835     & 0.280        & 0.47     \\
        TermPunct   & 句末标点比例  & 标量   & 直接使用                                 & 0.498     & 0.298        & 0.60     \\
        Ad-Prob     & 无广告概率   & 列表2  & softmax 取 $P(\mathrm{no\text{-}ad})$ & 0.491     & 0.291        & 0.20     \\
        MB-Reason   & 推理性     & 列表6  & 期望等级 $/5$                            & 0.404     & 0.251        & 0.60     \\
        UniqueWords & 词汇多样性   & 标量   & 直接使用                                 & 0.624     & 0.224        & 0.62     \\
        DSIR-Math   & 数学域倾向   & 标量   & 直接使用                                 & 0.626     & 0.223        & 0.64     \\
        DSIR-Books  & 书籍域倾向   & 标量   & 直接使用                                 & 0.625     & 0.225        & 0.63     \\
        DSIR-Wiki   & 维基域倾向   & 标量   & 直接使用                                 & 0.626     & 0.223        & 0.64     \\
        FineWebEdu  & 教育价值    & 列表1  & 取列表唯一评分                              & 0.447     & 0.279        & 0.24     \\
        MB-Prof     & 专业性     & 列表6  & 期望等级 $/5$                            & 0.452     & 0.268        & 0.69     \\
        Fluency     & 语言流畅度   & 列表2  & softmax 取 $P(\mathrm{fluent})$       & 0.477     & 0.293        & 0.30     \\
        QuRating    & 综合文本质量  & 列表4  & 四分量分位均值                              & 0.447     & 0.203        & 0.39     \\
        MB-Clean    & 洁净度     & 列表6  & 期望等级 $/5$                            & 0.435     & 0.283        & 0.44     \\
        MB-Read     & 可读性     & 列表6  & 期望等级 $/5$                            & 0.434     & 0.292        & 0.38     \\
        WordLength  & 平均词长    & 标量   & 直接使用                                 & 0.795     & 0.311        & 0.31     \\
        Numerical   & 数字字符比例  & 标量   & 直接使用                                 & 0.461     & 0.290        & 0.16     \\
        Sentences   & 句子数     & 标量   & 直接使用                                 & 0.857     & 0.270        & 0.38     \\
        WordCount   & 词数      & 标量   & 直接使用                                 & 0.858     & 0.271        & 0.42     \\
        Entropy     & 一元词熵    & 标量   & 直接使用                                 & 0.855     & 0.272        & 0.40     \\
        Top2Gram    & 二元重复集中度 & 标量   & 直接使用                                 & 0.420     & 0.282        & 0.27     \\
        Top3Gram    & 三元重复集中度 & 标量   & 直接使用                                 & 0.444     & 0.306        & 0.18     \\
        \bottomrule
    \end{tabular}
    \TableNote[0.94\textwidth]{注：字段形态中“列表 $m$”表示该字段为长度 $m$ 的列表；$\bar{s}$ 与 $\sigma_s$ 为统一表示 $s_{ij}$ 在 A1 全样本上的均值与标准差；$\eta_j$ 为指标 $j$ 的领域间方差占比；中心适宜指标在中间 60\% 分位区间取满分，其 $\bar{s}$ 相应偏高。表内按信息簇 C1--C9 分组排列。}
\end{table}

\subsubsection{域均衡层次质量评价}

A1 中 book 仅 171 条、arxiv 为 1,419 条，而 c4、github、stackexchange、wikipedia 均接近 $10^4$ 条。
若以全样本经验分布作为统一标尺，大样本领域将获得更高权重；若分别在域内归一化，则会抹去域间真实差异。
因此，对指标 $j$ 在领域 $d$ 内建立经验分布 $F_{jd}$，并令七个领域等权参与公共参考尺度，即
\begin{equation}
    F_j^{\mathrm{bal}}(x)=\frac{1}{7}\sum_{d=1}^{7}F_{jd}(x).
    \label{eq:q1_bal_ecdf}
\end{equation}
在此基础上，将不同方向的指标统一为 $s_{ij}\in[0,1]$，具体形式为
\begin{equation}
    s_{ij}=
    \begin{cases}
        F_j^{\mathrm{bal}}(x_{ij}),                           & j\in\mathcal P, \\[2pt]
        1-F_j^{\mathrm{bal}}(x_{ij}),                         & j\in\mathcal N, \\[2pt]
        \min\!\left\{\dfrac{F_j^{\mathrm{bal}}(x_{ij})}{0.20},
        \dfrac{1-F_j^{\mathrm{bal}}(x_{ij})}{0.20},1\right\}, & j\in\mathcal M,
    \end{cases}
    \label{eq:q1_suitability}
\end{equation}
其中 $\mathcal P,\mathcal N,\mathcal M$ 分别表示正向、负向和中心适宜指标集合。中心适宜函数在中间 60\% 分位区间取满分，仅对结构极端样本进行惩罚。

统一表征后，以域均衡权重计算 22 项信号的 Spearman 相关矩阵，并以 $1-|\rho|$ 为距离进行层次聚类。
综合轮廓系数和 120 次分层 bootstrap 的配对稳定度，数据驱动选择 $K=9$ 个信息簇。对第 $k$ 个信息簇 $G_k$，以簇内绝对相关矩阵的最大特征向量形成非负权重 $a_{jk}$；不同簇之间采用等权，即
\begin{equation}
    H_{ik}=\sum_{j\in G_k}a_{jk}s_{ij},\qquad
    Q_i^{(0)}=\frac{1}{K}\sum_{k=1}^{K}H_{ik},\qquad K=9.
    \label{eq:q1_q0}
\end{equation}
这种层次合成先在同类信号中提取共识，再汇总不同质量语义，从而削弱重复评价对综合分数的影响。
图~\ref{fig:q1_quality_structure} 表明，域均衡相关矩阵呈明显的簇状结构，顶部色带给出信息簇归属，右侧给出领域间方差占比 $\eta_j$ 与指标方向。
多个 DSIR 与模型评分信号的 $\eta_j$ 超过 0.60，说明其变化更多来自领域身份而非通用质量，因此这些指标只参与综合评价，不单独解释为领域质量的因果来源。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.78\textwidth]{figures/fig4_1_quality_structure.pdf}
    \caption{22 项质量信号的冗余结构与领域异质性}
    \label{fig:q1_quality_structure}
\end{figure}

\subsubsection{质量评价结果与一致性检验}
\label{subsubsec:q1_consistency}

领域级质量以域内 $Q_i^{(0)}$ 的均值表示，并在固定评价规则下采用分层 bootstrap 构造 95\% 置信区间。
表~\ref{tab:q1_q0_result} 给出七域点估计、分布中心和同源扩展检验，图~\ref{fig:q1_quality_evidence} 则展示领域排序及方向假设的主要证据。

\begin{table}[htbp]
    \TableSetup
    \caption{七域初始质量评价及同源扩展检验}
    \label{tab:q1_q0_result}
    \begin{tabular}{@{}C{2.30cm} C{1.25cm} C{1.85cm} C{2.65cm} C{1.85cm} C{1.60cm}@{}}
        \toprule
        领域                     & $n$                                             & A1 均值                                           & 95\% CI          & $\Delta Q$  & $W_1$    \\
        \midrule
        arxiv                  & 1,419                                           & 0.7076                                          & [0.7055, 0.7098] & $-0.001740$ & 0.002437 \\
        commoncrawl            & 9,640                                           & 0.6056                                          & [0.6043, 0.6067] & --          & --       \\
        stackexchange          & 10,000                                          & 0.6024                                          & [0.6011, 0.6038] & --          & --       \\
        wikipedia              & 10,000                                          & 0.5950                                          & [0.5934, 0.5968] & --          & --       \\
        c4                     & 10,000                                          & 0.5548                                          & [0.5529, 0.5566] & --          & --       \\
        book                   & 171                                             & 0.4994                                          & [0.4875, 0.5115] & --          & --       \\
        github                 & 10,000                                          & 0.4786                                          & [0.4771, 0.4802] & $-0.000054$ & 0.000384 \\
        \midrule
        \multicolumn{2}{c}{总体} & \multicolumn{2}{c}{$Q_{\mathrm{micro}}=0.5707$} & \multicolumn{2}{c}{$Q_{\mathrm{macro}}=0.5776$}                                             \\
        \bottomrule
    \end{tabular}
    \TableNote[0.94\textwidth]{注：A1 均值为初始质量 $Q^{(0)}$ 的域内均值，95\% CI 由分层 bootstrap 给出；$\Delta Q$ 为 A2/A3 全量均值减 A1 对应领域均值，$W_1$ 为 Wasserstein 距离。A2/A3 与 A1 同源，因此该结果用于检验抽样代表性。}
\end{table}

arxiv 的平均 $Q^{(0)}$ 最高，github 最低。github 的人工审阅相关仅为 0.071，明显低于其他领域，说明代码和技术文本与现有评价信号之间存在较强的领域失配；因此该排名仅表示统一评价口径下的相对位置，不作语料本身优劣的绝对判断。
A1 的微平均与宏平均接近，说明样本量不平衡未改变总体水平判断；book 样本量最小，其置信区间也最宽。
冻结 A1 的标量化规则、公共 ECDF、信息簇及簇内权重后应用于 A2/A3，arxiv 和 github 的均值差分别仅为 $-1.74\times10^{-3}$ 和 $-5.4\times10^{-5}$，对应的 Wasserstein 距离也很小，表明 A1 对两类同源扩展集合具有较好的代表性。

方向设定与聚合方式的消融结果见表~\ref{tab:q1_ablation}。最终“五中心”方案在保持七域排序稳定的同时，将 210 条分层人工复核与 $Q^{(0)}$ 的 Spearman 相关提高到 0.666；全部结构指标均按正向处理时，七域排序相关降至 0.429。
等权与 CRITIC 与本文方法的样本排序相关分别为 0.901 和 0.920，说明主要质量结构具有较高共识；全局 PCA 的一致性明显较低，说明直接以单一主成分压缩 22 项信号会损失部分质量语义。

\begin{table}[htbp]
    \TableSetup
    \caption{质量评价方案的消融与一致性检验}
    \label{tab:q1_ablation}
    \begin{tabular}{@{}C{1.60cm} C{3.20cm} C{1.70cm} C{1.70cm} C{3.10cm}@{}}
        \toprule
        检验类型 & 方案       & 七域排序           & 样本排序  & 人工审阅                        \\
        \midrule
        \multirow{4}{*}{方向设定}
             & 旧八中心方案   & 1.000          & 0.880 & 0.616 [0.54, 0.68]          \\
             & 最终五中心方案  & \textbf{1.000} & 1.000 & \textbf{0.666} [0.61, 0.72] \\
             & 中心指标减为四个 & 0.893          & 0.993 & 0.656 [0.60, 0.71]          \\
             & 全部按正向处理  & 0.429          & 0.772 & 0.634 [0.56, 0.70]          \\
        \addlinespace[2pt]
        \multirow{3}{*}{聚合基线}
             & 等权       & 0.929          & 0.901 & 0.619 [0.55, 0.69]          \\
             & CRITIC   & 0.929          & 0.920 & 0.625 [0.55, 0.69]          \\
             & 全局 PCA   & 0.750          & 0.622 & 0.449 [0.36, 0.53]          \\
        \bottomrule
    \end{tabular}
    \TableNote{注：三列均为 Spearman 相关 $\rho_s$；人工审阅列为 210 条分层样本上的 95\% 分层 bootstrap 区间，样本按领域与质量层次分层抽取，排序相关均以最终五中心方案为基准。}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.64\textwidth]{figures/fig4_2_quality_evidence.pdf}
    \caption{七域质量评价结果与评价规则可信度}
    \label{fig:q1_quality_evidence}
\end{figure}

表~\ref{tab:q1_ablation} 的人工审阅列同时给出 210 条复核样本上的 95\% 分层 bootstrap 区间。最终五中心方案相对 CRITIC、等权和旧八中心方案的审阅相关差值分别为 0.041、0.047 和 0.050，区间分别为 $[0.009,0.073]$、$[0.013,0.080]$ 和 $[0.017,0.083]$，均不包含 0，说明方向审计与层次合成带来的提升不是抽样噪声，但幅度有限。
相对“中心指标减为四个”的差值为 0.010，区间 $[-0.003,0.022]$ 包含 0，二者在人工一致性上不可区分，保留中心五指标的意义在于不丢失结构语义；相对“全部按正向处理”的差值区间同样包含 0，该方案的决定性缺陷体现在七域排序相关由 1.000 降至 0.429。

由此得到的 $Q^{(0)}$ 保留了样本间的质量次序和域间差异，可作为下一节冲突校正的基准分数；其数值用于统一评价口径下的相对比较。

% -----------------------------------------------------
% 4.2 质量冲突消解
% -----------------------------------------------------
\subsection{质量冲突消解}
\label{subsec:q1_conflict}

$Q^{(0)}$ 的合成默认各项质量证据均可直接使用，但特殊文本可能使个别评价器偏离该领域的常见关系。
因此，本文把“正常的多维差异”与“异常的不一致”分开处理。只有当某项观测显著偏离同域相关信号所预测的正常水平时，才记为质量冲突；随后降低该项证据在综合评价中的权重。

\subsubsection{质量冲突的域感知识别}

以上一节的域均衡相关结构为基础，仅保留 $|\rho|\ge0.10$ 且在 300 次分层 bootstrap 中稳定率不低于 0.95 的指标关系，共得到 135 条稳定边。
对每项指标，用其稳定邻居和领域信息建立 Elastic Net 预测器，并按七域进行 5 折 cross-fitting，使每个样本的条件期望均由未包含该样本的训练折给出。
记交叉预测残差为 $e_{ij}=s_{ij}-\widehat s_{ij}^{(-i)}$，并在所属领域内按 MAD 标准化。若异常度进入对应“指标--领域”经验残差分布的上 2.5\% 尾部，则置 $I_{ij}=1$。样本级冲突度同时计入异常覆盖范围和异常强度，即
\begin{equation}
    \begin{aligned}
        r_{ij}                 & =\frac{|e_{ij}|}{1.4826\,\mathrm{MAD}_{d_i}(e_{\cdot j})+\varepsilon}, \\
        p_{ij}^{\mathrm{conf}} & =1-F_{j,d_i}^{R}(r_{ij}),\qquad
        I_{ij}=\mathbf{1}\!\left\{p_{ij}^{\mathrm{conf}}<0.025\right\},                                 \\
        C_i                    & =\sqrt{\left(\frac{1}{22}\sum_j I_{ij}\right)
            \left(\frac{\sum_j I_{ij}r_{ij}}{\sum_j I_{ij}+\varepsilon}\right)}.
    \end{aligned}
    \label{eq:q1_conflict}
\end{equation}
该定义以各指标在各领域中的正常波动为参照，因此不会把代码、公式密集文本等领域特征仅因数值差异而判为冲突。

\subsubsection{冲突证据的可靠性重加权}

冲突不直接从样本总分中扣除，而是作用于对应证据的可靠度。设 $\tau_{j,d_i}$ 为指标 $j$ 在领域 $d_i$ 的冲突阈值，定义
\begin{equation}
    R_{ij}=\begin{cases}
        1,                                   & I_{ij}=0, \\
        \exp\{-0.5(r_{ij}-\tau_{j,d_i})_+\}, & I_{ij}=1,
    \end{cases}
    \qquad
    Q_i^*=\sum_{k=1}^{K}v_{ik}^*H_{ik},\quad
    v_{ik}^*=\frac{\sum_{j\in G_k}a_{jk}R_{ij}}
    {\sum_{\ell=1}^{K}\sum_{h\in G_\ell}a_{h\ell}R_{ih}}.
    \label{eq:q1_qstar}
\end{equation}
各信息簇可靠度的均值记为 $U_i$。当不存在冲突时，$v_{ik}^*=1/K$，故 $Q_i^*=Q_i^{(0)}$；出现局部异常时，仅相应信息簇的权重发生变化。

\subsubsection{冲突消解效果及稳健性}

冲突校正后，$Q^*$ 与 $Q^{(0)}$ 的全样本 Spearman 为 0.9992，说明总体质量次序基本保持不变；但修正量明显向高冲突样本集中。
Top 10\%、Top 5\% 和 Top 1\% 冲突样本的 $|Q^*-Q^{(0)}|$ 中位数分别为 0.00435、0.00655 和 0.0118，且 Top 10\% 样本贡献了 64.8\% 的总绝对修正量。图~\ref{fig:q1_conflict_resolution} 中的分箱中位线进一步显示，冲突程度升高后修正幅度呈持续上升趋势。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.66\textwidth]{figures/fig4_3_conflict_resolution.pdf}
    \caption{冲突修正的局部性与异常来源}
    \label{fig:q1_conflict_resolution}
\end{figure}

高严重度冲突主要集中在 Entropy、Sentences、Uppercase、WordCount 和 WordLength 等结构信号，表明异常主要来自文本结构偏离所属领域的常态。
最新结果中 GitHub 的平均冲突度最高（0.180），Book 最低（0.138）；这里的领域差异反映评价证据的一致性，而不用于比较领域质量高低。
将 A1 学得的关系结构、残差尺度和阈值冻结后应用于 A2/A3，严重冲突仍集中在同类结构信号。
作为对照，本文冲突度与“指标方差”和“最大两两差”的 Spearman 仅为 0.132 和 0.172；当显著性阈值在 0.01--0.10、可靠性衰减系数在 0.3--1.0 内变化时，$Q^*$ 排序与主方案的 Spearman 始终不低于 0.994。
因此，冲突校正主要影响高冲突尾部，并未改变质量评价的主体结构。第二小问最终输出 $(Q_i^*,U_i,C_i)$，分别记录修正后的质量、证据可信度和冲突程度。

% -----------------------------------------------------
% 4.3 领域配比—Loss关系
% -----------------------------------------------------
\subsection{领域配比--Loss关系}
\label{subsec:q1_mixture}

A4/A5 提供 512 组 1M 训练配方及其 13 域验证 Loss；A6/A7 为 1M 同尺度真实检验，A8/A9 和 A10/A11 分别提供 60M、1B 真实检验，A12--A15 为 10B、70B 外推表。
17 个训练比例满足 $p_i\ge0$、$\sum_i p_i=1$，而 13 个验证域的 Loss 基准不同，因此建模时需同时处理单纯形约束、多输出响应和跨尺度迁移。

\subsubsection{配比数据标准化及质量先验检验}

不同验证域的 Loss 先在 A5 内按验证域进行中位数--MAD 标准化，再定义总体配比表现，即
\begin{equation}
    z_{mk}=\frac{L_{mk}-\operatorname{median}(L_{\cdot k})}
    {1.4826\,\operatorname{MAD}(L_{\cdot k})},\qquad
    \phi(\mathbf p)=-\frac{1}{13}\sum_{k=1}^{13}\widehat z_k(\mathbf p).
    \label{eq:q1_phi}
\end{equation}
$\phi(\mathbf p)$ 越大，表示该配方在 13 个验证域上的总体表现越优。

第二小问得到的七域 $Q^*$ 经 A16 映射到 17 个训练域，记为 $\mathbf Q^{(17)}=(Q_1^{(17)},\ldots,Q_{17}^{(17)})$。由于 $\sum_i p_iQ_i^{(17)}$ 是 $\mathbf p$ 的确定函数，直接作为额外线性项会与配比变量共线。
因此以指数倾斜 $\widetilde p_i(\gamma)\propto p_i\exp[\gamma(Q_i^{(17)}-\bar Q)]$ 检验质量先验。交叉验证的最优值为 $\gamma=0$，且 $\gamma>0$ 时误差略有增加，说明 A4/A5 无法把“领域身份”与“领域质量”的独立作用分开识别。
本小问据此不引入质量先验，质量的独立边际效应转由校准生成的质量变化实验（B6、B7）估计；大规模扩展结果（B8）仅用于审视外推边界。

\subsubsection{配比--Loss 响应的预测与解释模型}

为显式刻画领域及领域组合的作用，对第 $k$ 个验证域建立二阶 Scheff\'e 混合响应
\begin{equation}
    \widehat z_k(\mathbf p)=\sum_{i=1}^{17}\beta_{ik}p_i+
    \sum_{1\le i<j\le17}\gamma_{ijk}p_ip_j,
    \qquad k=1,\ldots,13.
    \label{eq:q1_scheffe}
\end{equation}
17 个主效应和 136 个交互项在 13 个任务上联合拟合，以组稀疏约束筛选跨任务稳定结构，并用 Ridge 项稳定相关系数；一阶 Scheff\'e 和 ILR--Ridge 作为对照。
1M 真实检验结果见表~\ref{tab:q1_mixture_model}。

\begin{table}[htbp]
    \TableSetup
    \caption{1M 真实检验集上的配比响应模型比较}
    \label{tab:q1_mixture_model}
    \begin{tabular}{@{}C{3.40cm} C{2.20cm} C{2.00cm} C{1.60cm} C{2.80cm}@{}}
        \toprule
        模型              & 平均 RMSE        & 平均 $R^2$       & MAE            & 用途     \\
        \midrule
        一阶 Scheff\'e    & 0.522          & 0.619          & 0.435          & 线性混合基准 \\
        多任务稀疏 Scheff\'e & 0.444          & 0.710          & 0.361          & 机制解释   \\
        ILR--Ridge      & \textbf{0.420} & \textbf{0.765} & \textbf{0.337} & 数值预测   \\
        \bottomrule
    \end{tabular}
    \TableNote{注：RMSE、$R^2$、MAE 均为 13 个验证任务的平均值；总体宏观配比指标 $G$ 在 1M 检验集上的 $R^2$ 为 0.391。}
\end{table}

ILR--Ridge 的点估计最优；重复 5 折交叉验证下，其相对多任务 Scheff\'e 的 $R^2$ 差值均值为 0.038，95\% CI 为 $[-0.012,0.081]$，区间包含 0。
因此后续数值预测采用 ILR--Ridge，领域替换和交互机制采用多任务稀疏 Scheff\'e 解释，两类模型分别承担预测和机制分析任务。

\subsubsection{领域份额替换及互补结构}

在单纯形约束下，“增加领域 $i$”必然伴随其他领域份额减少，因此主效应系数不能直接解释为独立边际收益。
在参考配比 $\mathbf p$ 附近，定义从领域 $j$ 向领域 $i$ 转移份额的边际效应和两领域的总体互补指数，即
\begin{equation}
    S_{i\leftarrow j}(\mathbf p)=\frac{1}{13}\sum_{k=1}^{13}
    \left(\frac{\partial\widehat z_k}{\partial p_i}-\frac{\partial\widehat z_k}{\partial p_j}\right),\qquad
    C_{ij}^{\mathrm{mix}}=-\frac{1}{13}\sum_{k=1}^{13}\gamma_{ijk}.
    \label{eq:q1_sub_comp}
\end{equation}
$S_{i\leftarrow j}<0$ 表示在当前配比附近将份额从 $j$ 转移到 $i$ 有助于降低总体 Loss；$C_{ij}^{\mathrm{mix}}>0$ 表示两域共同出现时的 Loss 低于线性混合预期。
前者描述份额替换，后者描述非加性交互。

为控制稀疏配方造成的大系数伪影，正文只解释同时满足三项条件的关系，即两域训练覆盖充分、bootstrap 符号稳定率不低于 0.95、效应量位于训练支持范围。
约 52\% 的替换关系和 46\% 的互补关系通过该门槛。通过门槛的关系中，DM-Math $\rightarrow$ HackerNews 的替换效应为 $-2.68$，显示稳定的 Loss 改善信号；arXiv--GitHub 则表现出稳定互补。
相反，\texttt{enron\_emails} 的平均份额仅 0.0022、最大份额 0.026、非零占比 0.31，其大幅替换效应超出充分支持范围，因此不进入机制结论。
图~\ref{fig:q1_mixture_mechanism} 将替换矩阵和通过资格门槛的互补/冗余关系合并展示，灰色斜线区域表示证据不足。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.68\textwidth]{figures/fig4_4_mixture_mechanism.pdf}
    \caption{领域配比的替换机制与互补结构}
    \label{fig:q1_mixture_mechanism}
\end{figure}

\subsubsection{配比规律的跨尺度迁移边界}

同尺度预测精度不能直接代表跨规模可迁移性，因此分别考察配比空间外推和模型规模迁移。
在配比空间内，以 Hellinger 距离衡量测试配方到最近训练配方的距离。1M 检验集最近邻距离的中位数为 0.299，90\% 分位数为 0.375，且预测误差随距离增大而整体上升，说明模型的机制解释应限制在训练配方支持区域附近。

模型规模迁移通过不同尺度下的配方排序一致性衡量。表~\ref{tab:q1_cross_scale} 和图~\ref{fig:q1_transfer_boundary} 显示，60M 和 1B 的 Spearman 相关分别为 0.810 和 0.608，均保持为正，但随规模扩大明显减弱；10B、70B 的 Loss 为较小规模幂律外推结果，其排序相关和预测--观测斜率均转为负值。
因此，现有数据支持真实 1M--1B 范围内的有限迁移，不支持将小模型上的配比规律视为尺度不变关系。

\begin{table}[htbp]
    \TableSetup
    \caption{领域配比效应的跨尺度一致性}
    \label{tab:q1_cross_scale}
    \begin{tabular}{@{}C{1.90cm} C{1.90cm} C{1.40cm} C{2.40cm} C{2.00cm} C{2.00cm}@{}}
        \toprule
        模型规模 & 数据性质    & 样本数 & 排序相关 $\rho_s$ & 两两一致率 & 回归斜率     \\
        \midrule
        60M  & 真实检验    & 256 & 0.810         & 0.805 & 0.912    \\
        1B   & 真实检验    & 64  & 0.608         & 0.711 & 0.348    \\
        \addlinespace[2pt]
        10B  & 外推 Loss & 63  & $-0.584$      & 0.293 & $-0.339$ \\
        70B  & 外推 Loss & 63  & $-0.617$      & 0.279 & $-0.370$ \\
        \bottomrule
    \end{tabular}
    \TableNote[0.94\textwidth]{注：排序相关为各尺度配方排序与 1M 预测排序的 Spearman 相关，两两一致率为配方两两比较方向一致的比例，回归斜率为预测--观测回归的斜率；10B/70B 的 Loss 来自小规模实验的幂律外推，仅用于识别尺度失效边界，不作为真实大模型训练验证。}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.64\textwidth]{figures/fig4_5_transfer_boundary.pdf}
    \caption{配比模型的预测有效域与跨尺度迁移边界}
    \label{fig:q1_transfer_boundary}
\end{figure}

问题一最终得到三类后续输入，即冲突修正质量 $Q^*$、映射到 17 个训练域的质量描述 $Q^{(17)}$ 以及总体配比效应 $\phi(\mathbf p)$。
替换效应 $S_{i\leftarrow j}$ 和互补指数 $C_{ij}^{\mathrm{mix}}$ 用于解释领域结构，$U$ 与 $C$ 用于记录质量评价中的不确定性。
问题二将在此基础上融合附件 B，建立含参数规模、数据量、质量和领域结构的广义标度关系。

% =====================================================
% 五、问题二
% =====================================================
\section{问题二：广义标度律及语料规模---质量、配比替代}

\subsection{经典标度律的扩展动因与分析框架}

\subsubsection{经典标度律的局限}

经典标度律通常将训练 Token 视为同质资源，重点描述参数规模和数据规模对模型损失的影响。然而，随着高质量语料逐渐稀缺，不同数据在信息密度、噪声水平、知识覆盖和任务相关性方面呈现出显著差异，单纯增加参数量或训练 Token 数已不足以准确解释模型能力的增长规律。如果忽略这种数据异质性，模型不仅会高估低质量数据的边际价值，还会掩盖领域结构失衡造成的性能瓶颈，从而难以回答数据质量提升、领域配比调整与模型规模扩张之间能否相互替代这一关键问题。因此，将数据质量和领域配比纳入标度律，是从“规模驱动”转向“规模与数据价值协同驱动”的必要拓展，也是实现有限算力下精细化资源配置的理论基础。

这一局限在固定规模的比较中尤为清楚，当模型参数量和训练 Token 数相同时，经典律给出相同的损失基线，却不区分语料来源的替换与质量等级的改变。故规模项可以刻画投入增加的平均趋势，不能单独解释同等训练规模下的损失差异。本文以验证损失为可建模的响应量，讨论的“替代”也首先是达到相同目标损失所需资源的变化，而非将损失改善直接等同于所有下游能力的提升。

\subsubsection{数据结构与可辨识约束}

数据结构决定了这一拓展不能简单地用一个质量系数修正训练 Token 数。设训练数据来自 $m$ 个领域，配比 $\mathbf p$ 满足 $p_i\geq0$ 与 $\sum_{i=1}^{m}p_i=1$，任一来源份额的增加必然挤占其他来源。来源质量 $Q_i$ 由问题一的数据分级给定；在本文的 17 域实例中，$Q_i=Q_i^{(17)}$，并在现有配比实验中保持固定。若把完整配比压缩为平均质量 $\sum_i p_iQ_i$ 或单一有效数据量，质量等级相同但知识内容不同的来源将无法区分，跨评估目标的差异化响应也会被掩盖。同时，固定的 $Q_i$ 与来源身份交织，不能仅凭这些实验识别独立提高质量的效应。可观测的模型---数据规模组合较少，不同体系的原始损失又存在尺度偏移，因此盲目增加交互项或直接混合损失均会削弱参数的可辨识性。

因此，配比变化与质量提升必须采用不同的识别口径。前者是在给定来源质量下沿配比单纯形重新分配份额，其收益取决于被增加和被压缩的来源以及评估目标；后者需要单独变化整体质量等级的实验信息。在仅改变配比的记录中，$p_iQ_i$ 随 $p_i$ 变化并不意味着观测到了 $Q_i$ 的独立变化。将这两种机制区分开，才能避免把“更换训练来源”的效果误写成“提高同一来源质量”的效果。

\subsubsection{分阶段修正的标度律扩展框架}

基于上述约束，本文采用“经典规模基线---组成响应---独立质量校准”的分层建模框架。首先，对多来源损失进行尺度归一化，利用自适应结构早停识别稳健的经典标度指数。随后针对任意评估目标，在规模基线上叠加配比响应项，以质量加权份额 $p_iQ_i$ 表示分级质量与投入的联合暴露，以中心化对数比特征刻画单纯形上的相对组成，并允许响应幅度随 $N$、$D$ 变化。该设计保留了经典标度律对规模收益递减的解释，同时允许相同质量等级的不同来源产生不同的损失变化。受规模锚点数量限制，扩展模型沿用下文辨识的经典指数，而不从配比实验中重新辨识所有规模参数。

整体质量等级 $Q$ 的作用则由单独变化质量的实验标定，并以参考质量 $Q_0$ 与已辨识的规模---配比关系衔接。这样，组成响应与质量响应各有对应的信息来源，也能清楚标识二者联合变化所依赖的结构假设。在此基础上，固定模型规模和目标损失，比较不同 $Q$ 与可行配比 $\mathbf p$ 下所需的训练 Token 数 $D$，才能定义语料规模---质量、配比的等损失替代。该替代是给定工作点及模型适用范围内的条件关系，其边界将在后文单独讨论。

\subsection{损失尺度归一化与锚定分阶段标度律建模}

\subsubsection{经典标度律基线的辨识与适用边界}

为刻画模型规模、训练数据量与验证损失之间的基本数量关系，本文以真实训练日志（B1）辨识经典标度律，并将族外日志、插值轨迹及跨族和文献样本（B2--B5）保留为不同层级的检验证据。所采用的经典形式为
\begin{equation}
    L(N,D)=E+A N^{-\alpha}+B D^{-\beta},
    \label{eq:classic-scaling-law}
\end{equation}
其中，$N$ 表示模型参数量，$D$ 表示训练 Token 数，$L$ 表示验证集交叉熵损失。$E$ 是规模趋于充分时仍无法消除的不可约损失，$A$ 与 $B$ 分别控制参数规模项和数据规模项的幅度，$\alpha$ 与 $\beta$ 则刻画两类资源的边际收益衰减速度。为保证模型具有明确的物理含义，估计过程中施加
\begin{equation}
    0\leq E<\min_i L_i,\qquad
    A>0,\quad B>0,\quad \alpha>0,\quad \beta>0
\end{equation}
等约束。由式~\eqref{eq:classic-scaling-law}可知，随着 $N$ 或 $D$ 增大，预测损失单调下降，但改善幅度逐渐减小，因而能够自然描述大模型训练中的收益递减现象。

\paragraph{损失尺度归一化}

不同模型体系可能采用不同的分词器、验证语料和损失统计口径，即使其学习曲线具有相似形状，原始 Loss 的位置和尺度也可能存在系统差异。若直接比较原始损失，模型族之间的整体平移或缩放会被误判为标度结构失效。为此，本文将主拟合来源的 Loss 定义为参考尺度，并假设第 $j$ 个外部来源的观测损失满足仿射观测关系
\begin{equation}
    L_{ji}^{\mathrm{obs}}
    =a_j+b_j L_{ji}^{\mathrm{ref}}+\varepsilon_{ji},
    \qquad b_j>0,
    \label{eq:loss-affine-map}
\end{equation}
其中，$a_j$ 描述损失基线差异，$b_j$ 描述损失单位或动态范围差异，$\varepsilon_{ji}$ 为无法由统一尺度解释的结构残差。相应地，将外部观测映射到参考尺度：
\begin{equation}
    \widetilde L_{ji}
    =\frac{L_{ji}^{\mathrm{obs}}-a_j}{b_j}.
    \label{eq:loss-normalization}
\end{equation}
主拟合来源取 $a_0=0$、$b_0=1$，因此其训练过程保持在原始可解释的 Loss 尺度上。其他来源经式~\eqref{eq:loss-normalization}归一化后再参与验证与结构选择。

为防止利用同一批验证标签完成校准后又在原样本上评价，本文采用按训练轨迹或模型族分簇的交叉归一化。对于待评价簇 $c$，仅使用其余簇估计
\begin{equation}
    (\widehat a_j^{(-c)},\widehat b_j^{(-c)})
    =\arg\min_{a\in\mathbb R,\,b>0}
    \sum_{i:\,g(i)\neq c}
    \left[L_{ji}^{\mathrm{obs}}-
        \left(a+b\widehat L_{ji}^{\mathrm{ref}}\right)\right]^2,
    \label{eq:cross-fitted-normalization}
\end{equation}
再使用 $\widehat a_j^{(-c)}$ 和 $\widehat b_j^{(-c)}$ 归一化留出簇。该处理只消除来源间的仿射尺度差异，不改变留出轨迹的排序、曲率和局部偏差，因而归一化后的剩余误差仍能反映标度规律的跨体系可迁移性。为保持评价透明性，本文同时保留未经校准的原始尺度误差，并将其解释为“结构误差与观测尺度偏移”的合成结果。

\paragraph{可分离非线性最小二乘法}

式~\eqref{eq:classic-scaling-law}对五个参数整体而言是非线性模型，但当指数 $(\alpha,\beta)$ 给定后，$E$、$A$ 和 $B$ 构成线性参数。令
\begin{equation}
    \boldsymbol y=(L_1,\ldots,L_n)^{\mathsf T},\qquad
    \boldsymbol\theta=(E,A,B)^{\mathsf T},
\end{equation}
并构建设计矩阵
\begin{equation}
    \boldsymbol X(\alpha,\beta)=
    \begin{bmatrix}
        1      & N_1^{-\alpha} & D_1^{-\beta} \\
        1      & N_2^{-\alpha} & D_2^{-\beta} \\
        \vdots & \vdots        & \vdots       \\
        1      & N_n^{-\alpha} & D_n^{-\beta}
    \end{bmatrix}.
\end{equation}
于是，在任意候选指数对下，内层问题可写为带物理约束的线性最小二乘
\begin{equation}
    \widehat{\boldsymbol\theta}(\alpha,\beta)
    =\arg\min_{\boldsymbol\theta}
    \left\|\boldsymbol y-
    \boldsymbol X(\alpha,\beta)\boldsymbol\theta\right\|_2^2.
    \label{eq:inner-linear-ls}
\end{equation}
将内层解析解代回后，原问题被压缩为仅关于两个指数的轮廓残差平方和
\begin{equation}
    S(\alpha,\beta)
    =\left\|\boldsymbol y-
    \boldsymbol X(\alpha,\beta)
    \widehat{\boldsymbol\theta}(\alpha,\beta)\right\|_2^2.
    \label{eq:profile-sse}
\end{equation}
本文先在指数区间内设置等距节点进行粗网格搜索，再以当前最优点为中心实施局部模式搜索，并逐次将搜索步长减半，直至步长小于 $10^{-7}$。
相较于同时对五个参数进行无结构的非线性优化，该可分离非线性最小二乘方法将高维耦合问题转化为“外层低维指数搜索+内层线性求解”，既降低了初值敏感性，也便于在每个指数候选点严格检查参数正性和不可约损失边界。

\paragraph{自适应结构早停}

指数自由度越高，越容易把数据噪声读作真实的标度结构差异，因此本文按由简到繁、以外部验证收益为准的顺序逐层释放指数自由度。
第一层令 $\alpha=\beta=s$，对共享指数 $s$ 进行一维搜索。第二层以第一层最优值 $s^{\ast}$ 为锚点，分别考察“固定 $\beta=s^{\ast}$、释放 $\alpha$”和“固定 $\alpha=s^{\ast}$、释放 $\beta$”两个并列候选。只有当单方向释放产生稳定的外部收益时，第三层才进入 $(\alpha,\beta)$ 均自由的二维搜索。每个候选指数点仍由式~\eqref{eq:inner-linear-ls}估计 $E$、$A$ 和 $B$，因此该过程并未引入新的标度律项，而是在同一标准模型内部逐步增加指数自由度。

不同来源的可信度、判别难度与统计稳定性并不相同，因此本文不预先固定其权重。记 $r_j$ 为可信度先验，$d_j$ 为基准模型在该来源上的验证难度，$\sigma_j$ 为按轨迹或模型族分簇 Bootstrap 得到的分数标准差，则定义稳定性因子和原始权重
\begin{equation}
    s_j=\frac{1}{1+\sigma_j/d_j},
    \qquad
    u_j=r_j d_j^{\xi}s_j,
\end{equation}
并在给定上下限后将 $u_j$ 归一化为 $\omega_j$。较困难但稳定、且证据可靠的来源因而获得较高权重。高度相关、半合成或波动较大的来源则不会主导结构选择。权重在比较候选结构之前一次确定并冻结，以避免针对某个候选模型事后调整评价标准。

候选结构的最终选择分数定义为
\begin{equation}
    J_m=\sum_j \omega_j \operatorname{MedAPE}_{jm}
    +\lambda\max(0,k_m),
    \label{eq:early-stopping-score}
\end{equation}
其中，$\operatorname{MedAPE}$ 衡量归一化后的相对预测偏差, $k_m$ 为有效自由参数数目，第二项用于要求新增指数自由度给出足以抵消复杂度增长的外部收益。候选结构首先需要满足主拟合样本上的最低拟合门槛。随后，只有当$ J_m<J_{\mathrm{best}}-\delta $时，才将其视为实质改善并更新历史最优结构。

\paragraph{经典标度律的拟合、验证与外推}

自适应结构早停最终保留参数项与数据项共享指数的简约结构，得到
\begin{equation}
    \widehat L(N,D)
    =1.654679+0.425107N^{-0.293183}
    +1.205248D^{-0.293183},
    \label{eq:fitted-classic-scaling-law}
\end{equation}
其决定系数为 $0.99938498$，RMSE 为 $0.00848302$。释放参数规模指数仅使选模分数改善 $0.000159$，低于阈值 $\delta=0.002$，因此不增加指数自由度。图~\ref{fig:scaling-compute-pair}(a)表明，增加训练数据量或模型参数量均可降低损失，但边际收益持续递减。将资源投入统一换算为 $C=6ND$ 后，各规模训练轨迹呈现相近的下降趋势。图~\ref{fig:scaling-compute-pair}(b)中的经验下沿包络近似满足 $L=2.2C^{-0.0665}$，刻画了既定计算预算下可达到的效率前沿。

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.47\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/traditional_scaling_law_quad_ab.png}
        \caption{}
        \label{fig:classic-scaling-components}
    \end{subfigure}
    \begin{subfigure}[t]{0.47\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/traditional_scaling_law_loss_vs_steps_law_b.png}
        \caption{}
        \label{fig:compute-loss}
    \end{subfigure}
    \caption{经典标度律的资源标度与计算效率：(a) 参数规模、数据规模与验证损失的标度关系。(b) 不同参数规模下计算量与验证损失的关系，虚线为经验下沿包络。}
    \label{fig:scaling-compute-pair}
\end{figure}

为检验跨来源迁移能力，本文提取独立训练轨迹的最终收敛点，并采用留一轨迹或留一模型族的仿射尺度校准。表~\ref{tab:multi-source-validation}与图~\ref{fig:validation-extrapolation-pair}(a)显示，归一化后的四类数据均保持较高秩相关性。
公开文献观测的误差相对较高，但考虑数据质量、网络结构与训练策略造成的差异，仍在标准标度律的描述范围内。

\begin{table}[htbp]
    \centering
    \caption{经典标度律的多来源样本外验证结果}
    \label{tab:multi-source-validation}
    \small
    \begin{tabular}{lccccccc}
        \toprule
        验证来源      & 编号 & 样本数  & 归一化 RMSE & 中位相对误差 & 归一化 $R^2$ & Spearman 系数 \\
        \midrule
        独立模型族收敛终点 & B2 & 7    & 0.014636 & 0.70\% & 0.9950    & 1.0000      \\
        连续插值训练轨迹  & B3 & 4000 & 0.011445 & 0.28\% & 0.9996    & 0.9998      \\
        跨模型族实验    & B4 & 57   & 0.088138 & 1.57\% & 0.9240    & 0.9758      \\
        公开文献实验观测  & B5 & 44   & 0.127853 & 5.24\% & 0.7647    & 0.9280      \\
        \bottomrule
    \end{tabular}
\end{table}

图~\ref{fig:validation-extrapolation-pair}(b)进一步给出百亿参数以上的情景外推。经典标度律在固定数据规模下预测损失随参数量缓慢下降，而大模型样本的离散程度明显更高，表明超大规模区间还受到数据质量与训练配置的显著影响。因此，该图用于界定外推趋势和风险，不作为新的外部验证证据，并为后续引入质量变量与领域配比提供依据。

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.47\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/traditional_scaling_law_validation_a.png}
        \caption{}
        \label{fig:multi-source-validation}
    \end{subfigure}
    \begin{subfigure}[t]{0.47\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/traditional_scaling_law_extrapolation_a.png}
        \caption{}
        \label{fig:large-model-extrapolation}
    \end{subfigure}
    \caption{经典标度律的跨来源验证与大模型外推：(a) 损失尺度归一化后的多来源预测一致性，虚线为理想预测，阴影区域表示相对误差不超过 $10\%$；(b) 百亿参数以上模型区间的标度律外推，曲线按中位训练数据量计算。}
    \label{fig:validation-extrapolation-pair}
\end{figure}

\subsubsection{质量加权组成响应的广义律构造}

经典标度律揭示了参数规模与训练数据量的损失衰减规律，但其同质 Token 假设无法区分训练语料的质量和来源组成。为分析这一缺口，首先考察实验的可辨识结构，在模型规模与训练 Token 数均固定的条件下，存在数百种不同的语料配比，因而经典标度律对这些实验给出同一预测基线，却无法解释其损失差异。另有 $256$ 组配比在两个模型规模下逐一对应，可比较规模改变后配比响应的形状与幅度。这说明需要在规模基线之外单独建立组成响应，而非仅用总 Token 数代表全部训练数据。与此同时，观测实验仅提供三个不同的 $(N,D)$ 规模锚点，其中较大模型的 $N$ 与 $D$ 同时变化。若连同组成效应重新估计全部规模指数，将产生明显的参数混淆。因此沿用前文估计的 $\alpha$、$\beta$，将新增参数集中用于刻画组成效应及其跨规模变化。

其次，训练配比属于单纯形，提高某一来源的份额必然压缩其他来源，故各 $p_i$ 不是相互独立的投入量。仅以平均质量 $\sum_i p_iQ_i$ 概括语料，会把质量相近但知识内容不同的来源混为一谈。只用原始份额又难以直接表达来源之间的相对替代。基于这一约束，采用质量加权份额 $p_iQ_i$ 记录各来源的有效投入，并以中心化对数比刻画相对组成。但现有质量评分对每个来源固定，$p_iQ_i$ 与 $p_i$ 在统计上不能区分独立的质量改善效应，因此这一项只表示给定质量分级下的组成暴露，不能作质量因果效应解释。

最后，逐行配对的跨规模配比为检验“响应形状相对稳定、作用幅度随规模改变”的可分离假设提供了依据。用规模幂函数调节组成响应的幅度，既与经典标度律的形式协调，也避免为每个规模重新拟合一套配比函数。以参考规模归一化该幅度，并将组成响应在参考样本上中心化，可减少其与损失截距之间的混淆。由于配比维度随来源数增长，估计时还须施加正则化以控制方差。设训练语料包含任意 $m$ 个来源，$\mathbf p=(p_1,\ldots,p_m)$ 满足 $p_i\geq0$、$\sum_i p_i=1$，$\mathbf Q=(Q_1,\ldots,Q_m)$ 为相应来源质量；本文实例取 $m=17$ 和 $\mathbf Q=\mathbf Q^{(17)}$。对于任意给定的评估目标，由此提出统一的函数结构
\begin{equation}
    \begin{aligned}
        L(N,D,\mathbf p,\mathbf Q)
        ={} & E+A N^{-\alpha}
        +B D^{-\beta}
        +\left(\frac{N}{N_{0}}\right)^{-\eta}
        \left(\frac{D}{D_{0}}\right)^{-\zeta}
        \varphi(\mathbf Q,\mathbf p),
    \end{aligned}
    \label{eq:generalized-scaling-design}
\end{equation}
其中$(N_{0},D_{0})$ 为首个观测规模锚点。$E$、$A$ 和 $B$ 决定损失基线，$\alpha$、$\beta$ 为规模指数，$\eta$、$\zeta$ 刻画组成响应随规模变化的程度。两个规模比值在参考规模均为 $1$，故 $\varphi$ 直接表示该规模下的损失修正，其符号方向与问题一的总体表现指标 $\phi(\mathbf p)$ 不同。为避免平均质量 $\sum_i p_iQ_i$ 抹去同等级、不同知识来源之间的差别，将组成响应展开为
\begin{equation}
    \begin{aligned}
        \varphi(\mathbf Q,\mathbf p)
        ={} & \sum_{i=1}^{m}w_i
        (p_iQ_i-\bar m_i) +\sum_{i=1}^{m}w_{\mathrm{clr},i}
        \bigl(\operatorname{clr}_{\varepsilon}(\mathbf p)_i
        -\bar m_{\mathrm{clr},i}\bigr).
    \end{aligned}
    \label{eq:generalized-composition-response}
\end{equation}
这里 $w_i$ 与 $w_{\mathrm{clr},i}$ 分别为质量加权份额和相对配比的有效权重。$\bar m_i$、$\bar m_{\mathrm{clr},i}$ 是对应特征在参考规模拟合样本上的均值，使 $\varphi$ 在该样本上均值为零。其中，中心化对数比定义为
\begin{equation}
    \operatorname{clr}_{\varepsilon}(\mathbf p)_i
    =\log(p_i+\varepsilon)
    -\frac{1}{m}\sum_{j=1}^{m}\log(p_j+\varepsilon).
    \label{eq:generalized-clr-definition}
\end{equation}
式~\eqref{eq:generalized-composition-response}中的 $p_iQ_i$ 记录质量加权的来源投入，中心化对数比反映单纯形上的相对结构，$\varepsilon>0$ 用于处理零占比。
拟合时对这两组特征进行标准化与正则化，并将所得系数换算为式~\eqref{eq:generalized-composition-response}中的有效权重。
不同评估目标均采用式~\eqref{eq:generalized-scaling-design}的函数结构，但 $E$、$A$、$B$、$\eta$、$\zeta$ 及组成权重分别估计。

\subsubsection{锚定分阶段广义标度律参数辨识与跨规模验证}

式~\eqref{eq:generalized-scaling-design}由经典规模基线与受规模因子调制的组成响应相加而成。若联合估计全部参数，仅有三个规模锚点不足以同时辨识经典律指数和组成响应指数。此外，响应形状与幅度可相互吸收，造成参数化不唯一。为此，本文按图~\ref{fig:generalized-law-identification} 所示采用“锚定标准律—分阶段参数辨识—因子对比”的递进结构，在保留经典律约束的同时辨识新增的组成项。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.94\textwidth]{figures/sacling_law_param_estimate.pdf}
    \caption{广义标度律的三阶段参数辨识：标准律锚定规模指数，分阶段估计组成响应与基线，再由规模因子对比确定响应幅度指数。图中 $\phi$ 为组成损失修正的示意简写，正文记作 $\varphi$。}
    \label{fig:generalized-law-identification}
\end{figure}

\paragraph{锚定标准律}
沿用前文由充分规模变化的实验辨识出的共享指数 $\alpha=\beta=0.293183$，只借用指数而不直接移植损失基线参数。由此将广义模型的规模基线限制在 $\{1,N^{-\alpha},D^{-\beta}\}$ 张成的空间中，避免用稀疏的配比实验重新估计指数。该锚定仅约束资源收益衰减的形状。损失下界及两个规模项的系数仍由当前评估目标的数据确定，从而不强求不同目标共享损失水平。以下 $N,D$ 分别以十亿参数和十亿 Token 计。三个观测锚点依次为 $(10^{-3},1)$、$(0.06,1)$ 和 $(1,25)$，首个锚点记为 $(N_0,D_0)$。

\paragraph{分阶段参数辨识}
在参考锚点，$N,D$ 保持不变，配比之间的损失差异因而提供组成响应的识别信息。对式~\eqref{eq:generalized-composition-response}中的两组特征逐维标准化，以岭回归估计该响应，正则强度由调参折选定。将拟合响应在参考训练配比上中心化、换算至损失单位，并规定其幅度 $\kappa_1=1$：前者使组成项与常数基线在参考样本上分离，后者固定 $\kappa\varphi$ 的尺度规范。令 $s$ 表示锚点、$r$ 表示该锚点训练折中的配比，$\varphi_{sr}=\varphi(\mathbf Q,\mathbf p_{sr})$。其余幅度是损失对同一响应形状的投影斜率。扣除组成项后得到各锚点的规模基线：
\begin{equation}
    \begin{aligned}
        \kappa_s & =\frac{\sum_r(\varphi_{sr}-\bar\varphi_s)(L_{sr}-\bar L_s)}
        {\sum_r(\varphi_{sr}-\bar\varphi_s)^2},\quad s=2,3,                    \\
        b_s      & =\bar L_s-\kappa_s\bar\varphi_s,\quad s=1,2,3.
    \end{aligned}
    \label{eq:generalized-stagewise-projection}
\end{equation}
这样，原先耦合的形状与幅度被化为参考规模的正则化回归及跨规模的一元投影。再以非负最小二乘将三个 $b_s$ 投影回锚定的经典律基底：
\begin{equation}
    (\widehat E,\widehat A,\widehat B)
    =\arg\min_{E,A,B\geq0}\sum_{s=1}^{3}
    \left(b_s-E-A N_s^{-\alpha}-B D_s^{-\beta}\right)^2.
    \label{eq:generalized-baseline-identification}
\end{equation}
非负约束保持经典基线项的物理方向。由于组成修正可为负，仍须另行检查预测损失是否低于 $E$。各评估目标采用相同的函数结构，但分别估计其基线与组成权重。

\paragraph{因子对比}
对正的响应幅度取对数，得到 $\log\kappa_s=-\eta\log(N_s/N_0)-\zeta\log(D_s/D_0)$。由于前两个锚点的 $D$ 相同，其幅度比消去了数据量因子，可先辨识 $\eta$，再从第三个锚点的总幅度变化中扣除已知的参数规模效应，条件辨识 $\zeta$：
\begin{equation}
    \eta=-\frac{\log\kappa_2}{\log(N_2/N_1)},\qquad
    \zeta=\frac{-\log\kappa_3-\eta\log(N_3/N_1)}{\log(D_3/D_1)}.
    \label{eq:generalized-amplitude-identification}
\end{equation}
上述对比利用实验设计形成的单因子变化，而不依赖同时搜索两个指数。同一配比在前两个锚点的损失还应满足 $L_2(\mathbf p)=a+\lambda L_1(\mathbf p)$，其中 $\lambda=\kappa_2/\kappa_1$、$a=b_2-\lambda b_1$。这一配对关系消去了 $\varphi$，因而可在不依赖其具体函数形式的条件下检验可分离结构。配对回归的训练折 $R^2$ 中位数为 $0.9868$，留出配比的跨规模预测相对 RMSE 中位数为 $1.16\%$。

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[t]{0.55\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/generalized_law_domain_accuracy.png}
        \caption{}
        \label{fig:generalized-domain-accuracy}
    \end{subfigure}
    \begin{subfigure}[t]{0.34\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/generalized_law_surface_parity.png}
        \caption{}
        \label{fig:generalized-surface-parity}
    \end{subfigure}
    \caption{广义标度律的留出配比验证：(a) 各评估目标的相对 RMSE、总体 $R^2$ 与配比效应 $R^2$；(b) 三个观测规模的实测与预测损失对比，灰色标记为同一组系数下关闭配比项的预测，实线为理想一致线，阴影表示 $\pm5\%$ 误差带。}
    \label{fig:generalized-validation-pair}
\end{figure}

\paragraph{广义标度律验证} 对逐行配对的规模采用相同配比划分，第三个规模独立划分，留出折均不参与拟合或选模。在 $13$ 个评估目标、三个已观测规模的留出配比上，三个规模的相对 RMSE 分别为 $5.84\%$、$5.49\%$ 和 $4.44\%$，汇总为 $5.54\%$。中位绝对百分比误差为 $2.40\%$，按评估目标与规模中心化的配比效应 $R^2$ 为 $0.9006$。留出预测的最小 $\widehat L-E$ 为 $0.2129$，未跌破基线下界。图~\ref{fig:generalized-validation-pair}(a) 表明多数评估目标保持较低误差和较高解释度，但数学目标的误差明显偏大，提示跨目标性能仍有差异。图~\ref{fig:generalized-validation-pair}(b) 中，三个规模的留出预测总体沿理想一致线分布。关闭配比项后，预测在固定规模与评估目标内不再随配比变化，形成水平带，说明仅靠 $N,D$ 无法解释同规模的损失波动，这些结果支持已观测规模内的配比泛化。

\subsection{语料规模---质量、配比替代}

前述验证表明，资源规模项能够描述总体损失的衰减趋势，而语料质量与领域组成进一步决定相同 Token 预算下的损失差异。因而，讨论质量对规模的替代，首先需要在明确的工作点和可行配比方向上度量局部收益；不同实验尺度下的收益只有经过一致校准，才能进一步换算为等效 Token 数。

\subsubsection{各因素边际效用与弹性分析}

在损失为正、工作点位于可行域内部且局部可微时，对标量因素 $x\in\{N,D,Q\}$ 定义边际降损与损失弹性为
\begin{equation}
    g_x=-\frac{\partial L}{\partial x},\qquad
    \epsilon_x=\frac{\partial\ln L}{\partial\ln x}
    =\frac{x}{L}\frac{\partial L}{\partial x}.
    \label{eq:generalized-factor-elasticity}
\end{equation}
其中，$N,D$ 的响应由广义标度律给出；整体质量等级 $Q$ 不同于固定的来源质量 $Q_i^{(17)}$，其独立边际效应由质量实验拟合的响应模型估计，而不从广义律中的 $p_iQ_i$ 项直接推断。为使收益具有可解释的扰动尺度，规模因素报告 $L(x)-L(1.01x)$，质量因素报告 $L(Q)-L(Q+0.01)$。配比受单纯形约束，增加来源 $i$ 的份额 $t$ 时，令 $p_i(t)=p_i+t$、$p_j(t)=p_j\bigl(1-t/(1-p_i)\bigr)$（$j\ne i$），并以 $L(\mathbf p)-L(\mathbf p(0.01))$ 衡量可行方向的降损。

图~\ref{fig:factor-scale-quality-pair}(a) 给出固定参考配比下的规模弹性及配比方向收益。在已观测的 $N=10^9$、$D=2.5\times10^{10}$ 工作点，13 个评估目标的弹性中位数分别为 $\epsilon_N=-0.0345$、$\epsilon_D=-0.0868$。这表明在该点数据量的相对增益具有更高的损失敏感度，但尚不能据此比较参数与 Token 的算力效率。图中的百亿参数点仅是模型外推，不作为观测证据。图~\ref{fig:factor-scale-quality-pair}(b) 显示质量收益随 $Q$ 提高而递减：在 $Q=0.6$ 时，质量分增加 $0.01$ 的预测降损约为 $2.15\times10^{-3}$。主组 450 组与对照组 360 组存在包含关系，后者仅检验子集一致性。质量实验与广义律的损失基准尚未统一，故不能将两组导数直接相除，解释为质量提升所等效的 Token 增量。

\begin{figure}[htpb]
    \centering
    \begin{subfigure}[t]{0.4\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/figure_a_scale_mixture_sensitivity.png}
        \caption{}
        \label{fig:factor-scale-mixture-sensitivity}
    \end{subfigure}
    \begin{subfigure}[t]{0.4\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/figure_b_quality_response.png}
        \caption{}
        \label{fig:factor-quality-response}
    \end{subfigure}
    \caption{资源与质量因素的局部响应：(a) 参数量和数据量的损失弹性，以及配比份额增加 1 个百分点的方向收益分布；区间表示跨评估目标的四分位范围，百亿参数点仅为外推。(b) 独立质量实验中质量分增加 $0.01$ 的降损及损失弹性；对照组为主组子集。}
    \label{fig:factor-scale-quality-pair}
\end{figure}

\begin{figure}[htpb]
    \centering
    \begin{subfigure}[t]{0.45\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/figure_mixture_directional_gain.png}
        \caption{}
        \label{fig:factor-mixture-directional-gain}
    \end{subfigure}
    \begin{subfigure}[t]{0.45\textwidth}
        \centering
        \includegraphics[width=\linewidth]{figures/figure_mixture_share_sweep.png}
        \caption{}
        \label{fig:factor-mixture-share-sweep}
    \end{subfigure}
    \caption{领域配比的方向异质性与份额依赖：(a) 不同来源增配 1 个百分点时，各评估目标的预测降损，红色表示降损、蓝色表示增损，空白表示不满足可行方向条件；(b) 在观测支撑域内扫描来源份额所得的方向收益与弹性，色带反映跨评估目标的分布范围。两图均为模型敏感度分析，不作因果解释。}
    \label{fig:factor-mixture-pair}
\end{figure}

配比的边际效用不具有统一符号。图~\ref{fig:factor-mixture-pair}(a) 表明，同一来源的增配方向可能降低某些评估目标的损失，却提高另一些目标的损失；在参考配比下，117 个位于观测支撑域内的可行方向中，仅 $53.8\%$ 获得正收益。图~\ref{fig:factor-mixture-pair}(b) 进一步显示，收益还随来源份额改变，不能把单点导数延伸为全区间的固定排序。因此，配比调整应被视为面向目标和当前配方的局部资源重分配。

\subsubsection{广义标度律指导下的语料规模---质量、配比替代}

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.98\textwidth]{figures/joint_iso_loss_curves_two_domains.png}
    \caption{两种示例评估领域的质量---Token 等损失曲线。各曲线对应同一组观测配比，纵轴为保持参考目标损失所需的 Token 数；$Q_0=0.6$ 与 $Q_1=0.7$ 为质量比较点。离开 $N=10^9,D_\star=250$ 亿 Token 观测工作点后的需求量属于模型情景推算。}
    \label{fig:joint-iso-loss}
\end{figure}

\begin{figure}[htpb]
    \centering
    \includegraphics[width=0.98\textwidth]{figures/joint_mixture_effect_two_domains.png}
    \caption{同一组观测配比在两种示例评估领域中的等损失 Token 需求。各行配比保持一致，实心与空心标记分别对应 $Q_0=0.6$ 和 $Q_1=0.7$；横向差异表征模型预测的配比依赖性，并非配比干预的因果效应。}
    \label{fig:joint-mixture-effect}
\end{figure}

为将上一节的局部降损量转换为等损失的 Token 需求，以已辨识的广义标度律为主体，对数据受限项引入整体质量等级的外部标定：
\begin{equation}
    L(N,D,Q,\mathbf p)=E+AN^{-\alpha}
    +\left(\frac{Q}{Q_0}\right)^{-\gamma_Q}
    \left[ BD^{-\beta}+M(N,D)\varphi(\mathbf Q^{(17)},\mathbf p)\right].
    \label{eq:joint-quality-mixture-law}
\end{equation}
其中，$Q_0=0.6$ 是整体质量的校准点，$\mathbf Q^{(17)}$ 为问题一映射得到并在配比实验中固定的来源质量向量；$M(N,D)=(N/N_0)^{-\eta}(D/D_0)^{-\zeta}$ 为前述规模调制因子。质量指数 $\gamma_Q=0.1085$ 由单独变化质量的校准实验标定，其余参数均不重估。故 $Q=Q_0$ 时，式~\eqref{eq:joint-quality-mixture-law} 严格退化为原广义律。主口径假定质量同时作用于数据规模项与配比修正项；仅作用于 $BD^{-\beta}$ 的口径作为结构敏感性对照。主口径下，固定配比的精确局部等损失斜率为
\begin{equation}
    \left.\frac{\mathrm d\ln D}{\mathrm d\ln Q}\right|_{L,\mathbf p}
    =-\gamma_Q\frac{BD^{-\beta}+M\varphi}
    {\beta BD^{-\beta}+\zeta M\varphi},
    \label{eq:joint-iso-loss-slope}
\end{equation}
其中 $M\propto D^{-\zeta}$，$\varphi=\varphi(\mathbf Q^{(17)},\mathbf p)$。因此 $-\gamma_Q/\beta$ 仅在忽略配比项或两项具有相同数据量指数时成立，有限幅度的替代量须逐目标数值求解。

固定 $N=10^9$、替代工作点 Token 数 $D_\star=250$ 亿及参考配比 $\mathbf p_0$，将 $L(N,D_\star,Q_0,\mathbf p_0)$ 作为各评估目标的基准损失，求满足该目标损失时 $Q$ 与 $\mathbf p$ 对应的最小所需 $D$。这里 $D_\star$ 不同于广义律的规模归一化锚点 $D_0$。配比调整仅考虑观测支撑域内的方向：单一来源份额增加 $1$ 个百分点，其余来源按比例缩减。图~\ref{fig:joint-iso-loss} 展示两种示例评估领域的等损失曲线；在相同配比下提高质量使所需 Token 下降，而同一质量水平下不同配比对应不同的需求基线。图~\ref{fig:joint-mixture-effect} 用同一组真实配比进一步表明，这种配比效应的幅度具有明显的评估目标依赖性。

逐目标先对可行配比方向取节省量中位数，再对 13 个评估目标等权汇总：质量由 $0.6$ 提至 $0.7$ 时，固定配比的 Token 节省中位数为 $13.97$ 亿，叠加可行的 $1$ 个百分点配比调整后为 $14.06$ 亿；仅缩放规模项的敏感性口径分别为 $13.30$ 亿和 $13.59$ 亿。因而，小幅配比调整的总体增益有限，但图中的完整观测配比差异可显著改变所需 Token 水平，两种比较尺度不可混同。模型在 $Q_0$ 的锚定误差小于 $5\times10^{-16}$，可行求根的最大损失残差为 $2.33\times10^{-13}$。

\subsubsection{规模---质量、配比替代边界}

上述等损失换算由 $L(N,D,Q,\mathbf p)=L^\star$ 隐式定义，并非跨工作点通用的 Token 汇率。对式~\eqref{eq:joint-quality-mixture-law}，只有在 $BD^{-\beta}+M\varphi>0$ 且 $\beta BD^{-\beta}+\zeta M\varphi>0$ 时，提高质量和增加 Token 才同时具有降损效应，式~\eqref{eq:joint-iso-loss-slope} 的负斜率方可解释为局部替代。
由于组成修正 $\varphi$ 可正可负，这些条件须按评估目标、配比和工作点分别核查。识别边界同样重要：配比实验中的来源质量 $Q_i^{(17)}$ 固定，不能由 $p_iQ_i^{(17)}$ 单独辨识质量提升效应。
整体质量指数来自独立实验，其与组成项的乘性耦合是外部标定的结构假设，尚缺少同时独立改变质量与配比的实验验证。

独立改变整体质量等级的实验为局部质量响应提供了直接依据：在 $45$ 组固定 $(N,D)$ 的比较中，验证损失对 $Q$ 的线性趋势斜率均为负。包含 $450$ 条记录的拟合及其 $360$ 条记录的子集对照分别得到 $R^2=0.9568$ 和 $0.9535$；二者是包含关系，不能视为独立重复验证。在 $Q=0.6$ 的工作点，质量提高 $0.01$ 的预测降损约为 $2.15\times10^{-3}$，质量弹性约为 $-0.0482$，与负向的经验响应一致，但其边际收益随 $Q$ 提高而递减。相比之下，向更大模型和语料规模延伸的扩展结果在 $150$ 组固定 $(N,D)$ 的比较中给出全部为正的质量---损失斜率，并有 $362$ 条记录被截断于损失下界 $0.5$。这一方向冲突和截断现象表明，该扩展结果不能验证质量响应的外推，也不宜用于重标定质量指数；它并不构成“大模型中质量提升会增损”的实验证据。

因此，本文仅在观测支撑域内调整配比，并在独立质量实验标定的范围附近解释质量边际效用与弹性；质量与组成的联合替代仍须以两种耦合口径及逐目标差异进行敏感性检验。当等损失求解所需的 $D$ 偏离已观测规模，或将关系推广至更大模型时，所得节省量应视为模型情景推算，而非已实现的训练收益或因果效应。


% =====================================================
% 六、问题三
% =====================================================
\section{问题三：算力约束下的资源联合优化及结构性转移识别}
\begingroup
\normalsize
\emergencystretch=2em
\setlength{\parskip}{0pt}
\setlength{\textfloatsep}{4pt plus 1pt minus 1pt}
\setlength{\floatsep}{3pt plus 1pt minus 1pt}
\setlength{\intextsep}{4pt plus 1pt minus 1pt}
\setlength{\abovedisplayskip}{5pt}
\setlength{\belowdisplayskip}{5pt}
\setlength{\abovetopsep}{4pt}
\setlength{\belowcaptionskip}{2pt}
\captionsetup[subfigure]{font=small,skip=3pt}
\definecolor{QThreeBlue}{HTML}{3B5F98}
\definecolor{QThreeRed}{HTML}{BF3F4A}
\definecolor{QThreeGreen}{HTML}{5A8D36}
\renewcommand{\tabularxcolumn}[1]{m{#1}}
\tikzset{
    qthree box/.style={draw=black!60,fill=white,line width=.5pt,
        text width=4.25cm,minimum height=.92cm,inner sep=4pt,align=center},
    qthree heading/.style={text=white,text width=5.05cm,
        minimum height=.8cm,inner sep=0pt,align=center,font=\small\bfseries},
    qthree arrow/.style={-{Stealth[length=2.2mm]},line width=.8pt},
    qthree decision/.style={diamond,aspect=2.25,draw=QThreeRed,
        fill=QThreeRed!7,text width=2.65cm,inner sep=1.5pt,align=center}
}

\subsection{问题分析与联合优化框架}

前两问分别给出了领域质量评价和资源--损失响应，但资源的边际收益并不等于有限预算下的配置优先级。扩大模型、增加训练数据与提升质量均会占用算力，领域配比又会改变相同规模下的损失。因此，本问先在统一成本约束下求解联合配置，再比较质量成本和上下文长度的影响，最后沿预算变化识别配置策略是否发生结构性转移。

本文沿用第二问的13个评测域响应，将A4的512组真实配方作为配比约束依据，以B7质量响应描述整体治理收益，并采用C7的五档上下文长度构造外生情景。A6--A11用于复算前问响应在检验配方上的一致性，A12--A15用于说明跨尺度外推边界，不与真实训练观测混合。图~\ref{fig:q3_1}给出前问输入、联合求解和结果检验的衔接。

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[font=\small]
\filldraw[fill=QThreeBlue!12,draw=black!45] (-8.05,.4) rectangle (-2.95,-6.65);
\filldraw[fill=QThreeRed!10,draw=black!45] (-2.55,.4) rectangle (2.55,-6.65);
\filldraw[fill=QThreeGreen!14,draw=black!45] (2.95,.4) rectangle (8.05,-6.65);
\node[qthree heading,fill=QThreeBlue] at (-5.5,0) {前两问输入};
\node[qthree heading,fill=QThreeRed] at (0,0) {联合优化};
\node[qthree heading,fill=QThreeGreen] at (5.5,0) {结果检验};
\node[align=center] at (-5.5,-.78) {质量、配方与响应接口};
\node[align=center] at (0,-.78) {成本约束与配方支持域};
\node[align=center] at (5.5,-.78) {候选配置与结构判定};
\node[qthree box] (inone) at (-5.5,-1.6) {问题一：固定 $\mathbf Q^{(17)}$};
\node[qthree box] (intwo) at (-5.5,-3.05) {A4 真实配方：512 行};
\node[qthree box] (inthree) at (-5.5,-4.5) {问题二：13 域响应\\$L_k(N,D,\mathbf p)$};
\node[qthree box] (opone) at (0,-1.6) {外生 $L_{\mathrm{ctx}}$\\C7 五个可行值};
\node[qthree box] (optwo) at (0,-3.05) {决策变量：$N,D,\mathbf p,Q$};
\node[qthree box] (opthree) at (0,-4.5) {$C_{\mathrm{train}}+C_{\mathrm{attn}}+C_Q\le C$};
\node[qthree box] (opfour) at (0,-5.95) {$\mathbf p=A^{\mathsf T}w$\\$w\ge0,\quad\mathbf1^{\mathsf T}w=1$};
\node[qthree box] (checkone) at (5.5,-1.6) {多起点约束求解};
\node[qthree box] (checktwo) at (5.5,-3.05) {消融、敏感性\\与独立复算};
\node[qthree box] (checkthree) at (5.5,-4.5) {有限候选与\\结构转移判定};
\node[qthree box,draw=QThreeGreen,fill=QThreeGreen,text=white] (result) at (5.5,-5.95)
    {$N^*,D^*,\mathbf p^*,Q^*$\\及预测损失};
\draw[qthree arrow,QThreeBlue] (inone) -- (intwo);
\draw[qthree arrow,QThreeBlue] (intwo) -- (inthree);
\draw[qthree arrow,QThreeBlue] (inthree) -- (opthree);
\draw[qthree arrow,QThreeRed] (opone) -- (optwo);
\draw[qthree arrow,QThreeRed] (optwo) -- (opthree);
\draw[qthree arrow,QThreeRed] (opthree) -- (opfour);
\draw[qthree arrow,QThreeRed] (opthree) -- (checkthree);
\draw[qthree arrow,QThreeGreen] (checkone) -- (checktwo);
\draw[qthree arrow,QThreeGreen] (checktwo) -- (checkthree);
\draw[qthree arrow,QThreeGreen] (checkthree) -- (result);
\node[align=center,text width=15.8cm] at (0,-7.15)
    {$\mathbf Q^{(17)}$ 为前两问交接的固定域质量向量；$Q$ 为联合优化中的治理质量变量。};
\end{tikzpicture}
\caption{质量、配方及规模响应的衔接与联合求解流程}\label{fig:q3_1}
\end{figure}

记参数个数和训练Token数为 $N,D$，17域配比为 $\mathbf p=(p_1,\ldots,p_{17})$。第一问的 $\mathbf q=\mathbf Q^{(17)}$ 是固定领域属性，因跨域映射而只有6个不同取值；本问另以标量 $Q$ 表示整体治理质量，不将17个领域的质量提升分别设为自由变量。为衔接第二问的规模单位，定义
\begin{equation}\label{eq:q3_1}
n=\frac{N}{10^9},\qquad d=\frac{D}{10^9},\qquad
q_{\mathrm{mix}}=\sum_{i=1}^{17}p_iq_i,\qquad
Q_{\mathrm{base}}=\frac{1}{17}\sum_{i=1}^{17}q_i=0.6219326581.
\end{equation}

其中，$q_{\mathrm{mix}}$ 随配比变化，$Q$ 则通过质量收益和治理成本参与优化，二者不作等同。主分析取 $Q\in[Q_{\mathrm{base}},1]$；训练平均配方加权基线 $0.6193580512$ 用于敏感性比较，B7的校准点 $Q_0=0.6$ 仅作为质量响应标尺。表中 $N,D$ 统一以B表示（$1\,\mathrm B=10^9$），算力计算仍采用绝对个数。

\subsection{算力约束下的多维资源联合配置}

\subsubsection{基于质量桥接的联合资源配置建模}

领域配比属于组成数据，不能把各分量视为互不约束的投入。沿用第二问的特征构造，对配比作带平滑项的中心化对数比变换，并与质量加权份额拼接为34维特征：
\begin{equation}\label{eq:q3_2}
\operatorname{clr}_{\delta}(p)_i=\ln(p_i+\delta)-\frac{1}{17}\sum_{j=1}^{17}\ln(p_j+\delta),
\qquad \delta=0.001.
\end{equation}

\begin{equation}\label{eq:q3_3}
x=(p\odot\mathbf q,\operatorname{clr}_{\delta}(p)),\qquad
z=\frac{x-\mu}{\sigma},\qquad
\Phi_k=s_k(\beta_{0k}+\boldsymbol\theta_k^{\mathsf T}z-c_k).
\end{equation}

其中，$k=1,\ldots,13$ 对应评测域，$\mu,\sigma$ 为特征标准化参数，其余系数均取第二问估计结果。规模衰减项与基础损失响应为
\begin{equation}\label{eq:q3_4}
S_k=(n/0.001)^{-\eta_k}(d/1)^{-\zeta_k},\qquad
L_{2,k}=E_k+A_kn^{-\alpha}+B_kd^{-\beta}+S_k\Phi_k,
\end{equation}

这里 $\alpha=\beta=0.2931826439$。固定领域质量已通过 $p\odot\mathbf q$ 进入基础响应，整体治理带来的变化则由B7质量律补充：
\begin{equation}\label{eq:q3_5}
F_7(n,d,Q)=0.9654077057+0.5307187920n^{-0.2825}
              +1.6157571765(dQ)^{-0.108}.
\end{equation}

为使治理前后的响应在基线质量处一致，主模型采用比值桥接，并以加性桥接作为敏感性对照：
\begin{equation}\label{eq:q3_6}
\begin{aligned}
L_k^{(r)}&=L_{2,k}\left[\frac{F_7(n,d,Q)}{F_7(n,d,Q_{\mathrm{base}})}\right]^{\kappa},\\
L_k^{(a)}&=L_{2,k}+\kappa\left[F_7(n,d,Q)-F_7(n,d,Q_{\mathrm{base}})\right].
\end{aligned}
\end{equation}

两种形式在 $Q=Q_{\mathrm{base}}$ 时均还原第二问响应。主模型取 $\kappa=1$，另以 $0.5$、$1.5$ 检验收益强度，$\kappa=0$ 构成零收益对照。固定 $N,D,Q$ 时，质量项不改变配方排序；联合配置的收益来自配比、规模与治理投入在同一预算下的相互调整。

图~\ref{fig:q3_2}区分三个直接观测尺度与本问的条件预测配置；图~\ref{fig:q3_3}展示三类质量成本的差异。前者说明规模外推范围，后者说明质量收益必须与其额外开销共同考虑。
\begin{figure}[htbp]
\centering
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/raw_q3_scale_support_halfwidth.pdf}
\caption{直接支持尺度与联合配置}
\label{fig:q3_2}
\end{subfigure}\hfill
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/process_q3_quality_cost_halfwidth.pdf}
\caption{三种质量成本的增量曲线}
\label{fig:q3_3}
\end{subfigure}
\caption{直接观测尺度与联合配置及三类质量治理成本}\label{fig:q3_support_cost}
\end{figure}

单位Token的治理成本采用表~\ref{tab:q3_3}所列三种形式。治理只计算相对于基础质量的增量，不把原有数据质量重复计费。
\begin{table}[htbp]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{质量成本函数及题给参数}\label{tab:q3_3}
\begin{tabularx}{\textwidth}{@{}P{2.2cm}C{5.5cm}L@{}}\toprule
形式 & $g(Q)$ & 单位与说明 \\
\midrule
指数 & $10^7\exp(6Q)$ & FLOPs/Token，随质量凸增 \\
幂函数 & $5\times10^9Q^4$ & FLOPs/Token，四次幂 \\
对数 & $2\times10^9\ln(1+10Q)$ & FLOPs/Token，上界处有限 \\
\bottomrule\end{tabularx}
\end{table}

令 $L_{\mathrm{ctx}}$ 为外生上下文长度，将训练与注意力的合并系数及增量质量成本分别记为
\begin{equation}\label{eq:q3_7}
a=6+\eta_{\mathrm{attn}}L_{\mathrm{ctx}},\qquad
h(Q)=[g(Q)-g(Q_{\mathrm{base}})]_+,\qquad
\eta_{\mathrm{attn}}=2\times10^{-4}.
\end{equation}

训练、质量处理与注意力计算使用同一训练数据量 $D$，故总成本为
\begin{equation}\label{eq:q3_8}
C_{\mathrm{used}}=C_{\mathrm{train}}+C_{\mathrm{attn}}+C_Q
=6ND+\eta_{\mathrm{attn}}NDL_{\mathrm{ctx}}+Dh(Q)
=D[aN+h(Q)].
\end{equation}

设 $A\in\mathbb R^{m\times17}$ 为所选真实配方组成的锚点矩阵，$w$ 为非负权重，$\mathbf p=A^{\mathsf T}w$。以13域平均损失衡量整体表现，并将训练成本、质量边界和配方支持统一纳入约束。

\begin{samepage}
\noindent\textbf{问题三的联合配置模型写为}
\begin{equation}\label{eq:q3_9}
\begin{aligned}
\min_{N,D,Q,w}\quad &J(N,D,Q,w)=\frac{1}{13}\sum_{k=1}^{13}L_k(N,D,Q,A^{\mathsf T}w),\\
\mathrm{s.t.}\quad &\left\{
\begin{aligned}
&D\left[(6+\eta_{\mathrm{attn}}L_{\mathrm{ctx}})N+h(Q)\right]\le C, &&\text{(C1)}\\
&Q_{\mathrm{base}}\le Q\le1, &&\text{(C2)}\\
&10^6\le N\le10^{11},\quad 10^9\le D\le10^{13}, &&\text{(C3)}\\
&p=A^{\mathsf T}w,\quad w\ge0,\quad \mathbf1^{\mathsf T}w=1, &&\text{(C4)}\\
&L_k(N,D,Q,p)\ge10^{-6},\quad k=1,\ldots,13. &&\text{(C5)}
\end{aligned}\right.
\end{aligned}
\end{equation}
\end{samepage}

约束(C1)限定总FLOPs，(C2)限定治理质量，(C3)给出规模计算范围，(C4)使配比位于真实配方的凸包内，(C5)排除非正损失预测。由此，优化对象是四类资源的联合配置，而非四个独立目标；$L_{\mathrm{ctx}}$ 仅改变给定情景下的成本，不参与内点寻优。

\subsubsection{基于真实配方凸包的约束求解}

参数量与数据量横跨多个数量级，直接以原始数值求解容易造成搜索尺度失衡。本文采用对数参数化，将规模坐标归一化至 $[0,1]$，并根据预算计算可行质量上界：
\begin{equation}\label{eq:q3_16}
\begin{aligned}
Q_{\mathrm{hi}}&=\max\{Q\in[Q_{\mathrm{base}},1]:D_{\min}[aN_{\min}+h(Q)]\le C\},\\
N&=\exp\{\ln N_{\min}+u\ln(N_{\mathrm{cap}}/N_{\min})\},\\
D&=\exp\{\ln D_{\min}+v\ln(D_{\mathrm{cap}}/D_{\min})\},\qquad u,v\in[0,1].
\end{aligned}
\end{equation}

其中，$N_{\mathrm{cap}}$、$D_{\mathrm{cap}}$ 为规模范围与预算共同限定的搜索上界。主求解器采用SLSQP，设置三个确定性起点，并在满足预算、质量边界、凸组合及13域损失为正的候选中选择目标值最低者；独立复算采用COBYQA。

初始搜索使用25个锚点以降低权重维度，但其凸包只是完整A4凸包的子集，不能据此排除其他真实配方方向上的改善。为检查锚点缩减的影响，固定 $N,D,Q$，对全部512个配方沿下式作方向扫描：
\begin{equation}\label{eq:q3_17}
p(t)=(1-t)p+ta_r,\qquad r=1,\ldots,512,\qquad t\in\{10^{-4},10^{-3}\}
\end{equation}

这里 $a_r$ 为第 $r$ 个真实配方。发现下降方向后，将相应配方加入锚点集合并重新求解。由于 $p$ 和 $a_r$ 均属于完整A4凸包，增广过程始终保留真实配方支持约束。图~\ref{fig:q3_4}给出候选增广与复核流程；该检查用于寻找遗漏方向，不构成连续域全局最优证明。
\begin{figure}[htbp]
\centering
\begin{tikzpicture}[font=\small]
\filldraw[fill=QThreeBlue!12,draw=black!45] (-8.05,.4) rectangle (-2.95,-7.6);
\filldraw[fill=QThreeRed!10,draw=black!45] (-2.55,.4) rectangle (2.55,-7.6);
\filldraw[fill=QThreeGreen!14,draw=black!45] (2.95,.4) rectangle (8.05,-7.6);
\node[qthree heading,fill=QThreeBlue] at (-5.5,0) {支持域建立};
\node[qthree heading,fill=QThreeRed] at (0,0) {改善方向扫描};
\node[qthree heading,fill=QThreeGreen] at (5.5,0) {候选复核};
\node[align=center] at (-5.5,-.8) {真实配方与候选锚点};
\node[align=center] at (0,-.8) {有限方向与重新求解};
\node[align=center] at (5.5,-.8) {场景稳健性与识别边界};
\node[qthree box] (source) at (-5.5,-1.6) {A4 真实配方：512 行};
\node[qthree box] (anchors) at (-5.5,-3.1) {25 个初始锚点 /\\97 个扩展锚点};
\node[qthree box] (hull) at (-5.5,-4.65) {有限凸组合\\$\mathbf p=A^{\mathsf T}w$\\$w\ge0,\quad\mathbf1^{\mathsf T}w=1$};
\node[qthree box] (scan) at (0,-1.6) {固定 $N,D,Q$\\扫描 512 个真实配方};
\node[qthree box] (step) at (0,-3.1) {两步长方向检查\\$t\in\{10^{-4},10^{-3}\}$};
\node[qthree decision] (decision) at (0,-4.75) {发现严格\\改善？};
\node[qthree box] (keep) at (-5.5,-6.65) {否：保留候选\\并记录方向};
\node[qthree box] (expand) at (0,-6.65) {是：加入至多 4 个配方\\重新求解};
\node[qthree box] (feasible) at (5.5,-1.6) {可行、有限且\\各域 Loss 为正};
\node[qthree box] (perturb) at (5.5,-3.1) {预算、上下文\\与成本情景复核};
\node[qthree box] (equivalent) at (5.5,-4.65) {近等价配置与\\跨阈值反例审查};
\node[qthree box,draw=QThreeGreen,fill=QThreeGreen,text=white] (audit) at (5.5,-6.65)
    {有限候选库\\推荐配置与复核结果};
\draw[qthree arrow,QThreeBlue] (source) -- (anchors);
\draw[qthree arrow,QThreeBlue] (anchors) -- (hull);
\draw[qthree arrow,QThreeBlue] (hull.east) -- (-2.75,-4.65) -- (-2.75,-1.6) -- (scan.west);
\draw[qthree arrow,QThreeRed] (scan) -- (step);
\draw[qthree arrow,QThreeRed] (step) -- (decision);
\draw[qthree arrow,QThreeRed] (decision.south) -- (expand.north);
\draw[qthree arrow,QThreeRed] (decision.west) -- (-2.75,-4.75) -- (-2.75,-5.8) -| (keep.north);
\draw[qthree arrow,QThreeRed] (expand.east) -- (2.75,-6.65) -- (2.75,-1.6) -- (scan.east);
\draw[qthree arrow,QThreeGreen] (feasible) -- (perturb);
\draw[qthree arrow,QThreeGreen] (perturb) -- (equivalent);
\draw[qthree arrow,QThreeGreen] (equivalent) -- (audit);
\draw[qthree arrow,QThreeRed] (keep.south) -- (-5.5,-7.3) -- (8.2,-7.3) -- (8.2,-1.6) -- (feasible.east);
\node[align=center,text width=15.8cm] at (0,-8.15)
    {方向扫描用于检查候选遗漏；最终配置仍须满足可行性与数值稳定性要求。};
\end{tikzpicture}
\caption{真实配方约束下的候选增广与求解复核流程}
\label{fig:q3_4}
\end{figure}

为使配置结果具有可比性，主实验在相同预算、上下文和成本条件下设置四组对照：仅优化 $N,D$ 的规模基线，进一步释放配比的配比组，释放治理质量的质量组，以及同时释放两者的联合组。11档预算、5档上下文和3类成本共形成165个情景、660项求解，其他敏感性与复算安排见表~\ref{tab:q3_5}。
\begin{table}[htbp]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{四组消融与条件实验矩阵}\label{tab:q3_5}
\begin{tabularx}{\textwidth}{@{}P{3.6cm}P{6.8cm}L@{}}\toprule
类别 & 自由变量或变化因素 & 数量及用途 \\
\midrule
规模基线 & $N,D$；固定平均配方与基础质量 & 联合配置的基准 \\
配比组 & $N,D,p$；固定基础质量 & 分离配比调整收益 \\
质量组 & $N,D,Q$；固定平均配方 & 分离治理收益及成本 \\
联合组 & $N,D,p,Q$ & 评价联合资源配置 \\
主矩阵 & 11预算、5上下文、3成本、4组 & 660项 \\
成本对照 & 统一 $Q_{\mathrm{base}}$ 至 1 的端点增量价格 & 检验价格尺度影响 \\
桥接对照 & 2种桥接、3种强度、2种基线质量 & 检验接口影响 \\
搜索敏感性 & 49/97锚点、6起点、边界扰动 & 检验求解稳定性 \\
零收益与预算加密 & $\kappa=0$；41档预算 & 检验治理收益与阈值 \\
算法对照 & 5预算、2上下文、3成本、2求解器 & 60项 \\
\bottomrule\end{tabularx}
\end{table}

\FloatBarrier
\subsubsection{多预算配置结果与逐域权衡分析}

表~\ref{tab:q3_6}给出上下文4096、预算为 $10^{19}$、$10^{22}$、$10^{24}$ FLOPs时的代表性配置。各行均从相同响应与约束下的可行候选中选取，候选来源分别列明，避免将原25锚点结果与增广后的结果混为一谈。
\begin{table}[htbp]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{上下文4096时有限候选集中的典型资源配置}\label{tab:q3_6}
\begin{tabularx}{\textwidth}{@{}P{2.1cm}C{1.0cm}Y Y Y Y C{2.4cm}@{}}\toprule
$C$（FLOPs） & 成本 & $N$（B） & $D$（B） & $Q$ & $J$ & 候选来源 \\
\midrule
$10^{19}$ & 指数 & 0.0678 & 21.64 & 0.6219 & 2.38753 & 扩展锚点 \\
$10^{19}$ & 幂 & 0.0678 & 21.64 & 0.6219 & 2.38753 & 扩展锚点 \\
$10^{19}$ & 对数 & 0.0678 & 21.64 & 0.6219 & 2.38753 & 扩展锚点 \\
$10^{22}$ & 指数 & 2.5115 & 491.40 & 0.9829 & 1.66994 & 扩展锚点 \\
$10^{22}$ & 幂 & 2.6199 & 452.13 & 1.0000 & 1.67192 & 扩展锚点 \\
$10^{22}$ & 对数 & 2.2175 & 626.41 & 1.0000 & 1.66062 & 97锚点 \\
$10^{24}$ & 指数 & 21.9273 & 6529.84 & 1.0000 & 1.46600 & 97锚点 \\
$10^{24}$ & 幂 & 22.0068 & 6480.02 & 1.0000 & 1.46612 & 97锚点 \\
$10^{24}$ & 对数 & 21.5744 & 6758.48 & 1.0000 & 1.46547 & 97锚点 \\
\bottomrule\end{tabularx}
\end{table}

低预算时，三类成本均选择基础质量，新增算力主要用于参数量与数据量；预算提高后，治理质量逐步进入内点或达到上界。相同预算下，成本形式改变了治理投入与数据量之间的取舍，例如 $10^{22}$ FLOPs时指数成本对应 $Q=0.9829$，而另两类成本达到质量上界。这里的上界解是当前质量响应与成本共同作用的结果，并不表示真实语料已经达到完美质量。

完整配方见表~\ref{tab:q3_7}。以幂成本为例，预算提高时wikipedia en与gutenberg pg 19的份额增加，stackexchange的份额下降；pile cc则先增后减。这表明配方调整并非对某一来源持续加码，而是随规模和治理投入共同变化。
\begin{table}[H]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{幂成本条件下典型有限候选的完整领域配方（\%）}\label{tab:q3_7}
\begin{tabularx}{\textwidth}{@{}P{5.9cm}Y Y Y@{}}\toprule
领域 & $10^{19}$ & $10^{22}$ & $10^{24}$ \\
\midrule
arxiv & 3.45 & 2.62 & 1.79 \\
freelaw & 7.67 & 7.44 & 5.25 \\
nih exporter & 0.02 & 0.01 & 4.88 \\
pubmed central & 2.93 & 1.46 & 3.63 \\
wikipedia en & 6.84 & 7.74 & 19.69 \\
dm mathematics & 4.73 & 5.06 & 0.84 \\
github & 11.40 & 12.92 & 12.12 \\
philpapers & <0.005 & <0.005 & <0.005 \\
stackexchange & 14.07 & 13.10 & 3.83 \\
enron emails & 0 & <0.005 & 0.08 \\
gutenberg pg 19 & 6.55 & 9.03 & 15.90 \\
pile cc & 17.87 & 28.14 & 11.49 \\
ubuntu irc & 5.10 & 5.13 & 14.81 \\
europarl & <0.005 & <0.005 & 0.09 \\
hackernews & 6.16 & 4.63 & 5.22 \\
pubmed abstracts & 9.81 & 0.05 & <0.005 \\
uspto backgrounds & 3.41 & 2.67 & 0.36 \\
\bottomrule\end{tabularx}
\end{table}

对全部A4配方作方向检查后，165个原联合配置均发现初始25锚点凸包之外的改善方向。上下文4096的33组增广求解最终使用39--42个锚点，较原联合解进一步改善 $0.8383\%$--$2.5512\%$，配比的 $L_1$ 距离变化为 $0.5280$--$0.6468$。图~\ref{fig:q3_7}说明初始搜索范围对配置有实质影响；这部分收益单独报告，不并入原四组消融结果。
\begin{figure}[htbp]
\centering
\includegraphics[width=.98\textwidth]{figures/result_q3_hull_expansion.pdf}
\caption{上下文4096时33组锚点增广配置相对原25锚点联合解的变化}\label{fig:q3_7}
\end{figure}

整体损失下降也不意味着每个评测域同时改善。原165组联合情景中，162组至少有一个评测域损失上升，最多涉及2个领域；最大退步为 $0.0867182$ nats，出现在 $C=3\times10^{20}$、上下文2048、幂成本条件下的dm mathematics。图~\ref{fig:q3_6}保留13域的变化，说明均值目标下存在领域间权衡，不能仅凭平均改善判断全部任务受益。
\begin{figure}[htbp]
\centering
\includegraphics[width=.82\textwidth]{figures/result_q3_domain_tradeoffs.pdf}
\caption{典型配置的逐域代价；热力图保留13个评测域和9组预算--成本组合，颜色表示相对原配方的Loss变化}
\label{fig:q3_6}
\end{figure}

\FloatBarrier
\subsubsection{联合配置的消融与数值稳健性检验}

首先在原25锚点条件下比较四组配置，以相同预算、上下文和成本的规模基线为参照，定义平均改善率及非加性项
\begin{equation}\label{eq:q3_18}
G=100\frac{J_{\mathrm{base}}-J_{\mathrm{joint}}}{J_{\mathrm{base}}},\qquad
H=J_{\mathrm{quality}}+J_{\mathrm{mixture}}-J_{\mathrm{base}}-J_{\mathrm{joint}}.
\end{equation}

图~\ref{fig:q3_5}给出指数成本、上下文4096的四组轨迹。165个情景中，联合配置相对规模基线的平均损失改善为 $4.1603\%$--$5.8086\%$；$H$ 在71组为正、47组为负、47组接近零。因此，配比和治理的联合收益取决于预算与响应曲率，不能把两者单独优化的收益直接相加。上述改善均为响应模型预测。
\begin{figure}[htbp]
\centering
\includegraphics[width=.72\textwidth]{figures/result_q3_loss_ablation.pdf}
\caption{指数成本、上下文4096时11档预算的四组消融；各组均联动优化N/D}
\label{fig:q3_5}
\end{figure}

其次，质量收益的幅度与桥接形式可能影响候选排序。对同一候选集分别采用两种桥接和三种正收益强度，构成六个情景。设 $X$ 为共同可行候选集，以各情景中的最佳候选为参照，定义相对后悔值
\begin{equation}\label{eq:q3_21}
J_s^{\mathrm{best}}=\min_{x\in X}J_s(x),\qquad
R_s(x)=\frac{J_s(x)-J_s^{\mathrm{best}}}{|J_s^{\mathrm{best}}|}.
\end{equation}

再以最小化最坏情景后悔值选择稳健候选：
\begin{equation}\label{eq:q3_22}
x_{\mathrm{rob}}\in\mathop{\arg\min}_{x\in X}\max_sR_s(x),\qquad
v_{\mathrm{rob}}=\min_{x\in X}\max_sR_s(x).
\end{equation}

165组中的最大相对后悔值为 $1.5465\%$，在另设的零收益压力情景下为 $2.5946\%$。图~\ref{fig:q3_12}表明，候选对已考察的质量收益变化保持有限幅度的稳定性；这一结论限定于共同候选集和给定情景集合。

最后，以相同25锚点、相同三个起点和相同配置进行SLSQP与COBYQA的30对复算。60个结果均满足基本可行条件，59个通过扰动检查，其中SLSQP为30/30、COBYQA为29/30，最大均值损失差为 $0.000168474$。图~\ref{fig:q3_13}同时比较损失差与函数调用次数，用于检查结果对求解器的依赖，而非证明全局最优。
\begin{figure}[H]
\centering
\begin{subfigure}[b]{\textwidth}
\centering
\includegraphics[width=.98\linewidth]{figures/result_q3_finite_regret.pdf}
\caption{有限候选的情景后悔值}
\label{fig:q3_12}
\end{subfigure}\par\medskip
\begin{subfigure}[b]{\textwidth}
\centering
\includegraphics[width=.98\linewidth]{figures/process_q3_solver_agreement.pdf}
\caption{两种求解器的一致性}
\label{fig:q3_13}
\end{subfigure}
\caption{有限情景下的稳健配置与求解器一致性检验}
\label{fig:q3_robust_solver}
\end{figure}

\FloatBarrier
\subsection{质量成本与上下文长度的配置影响}

\subsubsection{基于边际弹性的资源替代关系}

联合配置的变化需要从预算约束下的边际收益解释。当总预算约束取等号、其余相关边界未激活时，可消去 $D$。提高质量一方面直接降低损失，另一方面增加单位Token成本并压缩可训练的数据量，因此
\begin{equation}\label{eq:q3_12}
D=\frac{C}{aN+h(Q)},\qquad
\frac{\mathrm dD}{\mathrm dQ}=-\frac{Dg'(Q)}{aN+h(Q)},\qquad
T_Q=J_Q-\frac{J_DDg'(Q)}{aN+h(Q)}.
\end{equation}

$T_Q$ 是沿预算边界提高质量时的净损失变化。若 $T_Q<0$，局部增加治理质量仍有利；内点驻值要求 $T_Q=0$。在下界处，$T_Q\ge0$ 对应继续治理缺乏局部收益；上界处则应结合相应的单侧条件判断，不能仅凭质量触及上界认定模型失效。

对未触及规模边界的配置，$N,D$ 的驻点条件为
\begin{equation}\label{eq:q3_13}
J_N+\lambda aD=0,\qquad J_D+\lambda(aN+h)=0.
\end{equation}

进而得到损失弹性与治理成本份额之间的关系：
\begin{equation}\label{eq:q3_14}
\varepsilon_N=-\frac{NJ_N}{J},\qquad
\varepsilon_D=-\frac{DJ_D}{J},\qquad
\frac{\varepsilon_N}{\varepsilon_D}=\frac{aN}{aN+h}=1-s_Q,
\end{equation}

\begin{equation}\label{eq:q3_15}
s_Q=\frac{C_Q}{C_{\mathrm{used}}}=\frac{h(Q)}{aN+h(Q)},\qquad
\left.\frac{\partial s_Q}{\partial N}\right|_{Q,L_{\mathrm{ctx}}}
=-\frac{ah(Q)}{[aN+h(Q)]^2}.
\end{equation}

式~\eqref{eq:q3_14}说明，治理成本占比越高，可分配给模型与数据规模的相对空间越小；式~\eqref{eq:q3_15}则表明，固定质量和上下文且 $h(Q)>0$ 时，模型规模增加会降低治理成本份额。因此，质量治理的绝对投入与预算占比不能互相替代。

图~\ref{fig:q3_8}给出三项成本分解。以增广后的幂成本配置为例，预算从 $10^{22}$ 增至 $10^{24}$ FLOPs时，治理占比由 $19.2241\%$ 降至 $2.6935\%$，绝对成本却从 $1.9224\times10^{21}$ 增至 $2.6935\times10^{22}$ FLOPs。图~\ref{fig:q3_9}进一步复核原165组联合配置的局部资源均衡关系，最大归一化残差为 $2.04434\times10^{-6}$。
\begin{figure}[H]
\centering
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/result_q3_cost_allocation_halfwidth.pdf}
\caption{训练、注意力及治理份额}
\label{fig:q3_8}
\end{subfigure}\hfill
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/process_q3_resource_balance_halfwidth.pdf}
\caption{资源均衡残差}
\label{fig:q3_9}
\end{subfigure}
\caption{训练成本分解与局部资源均衡检验}
\label{fig:q3_cost_balance}
\end{figure}

\subsubsection{基于端点归一化的质量成本比较}

三类成本不仅曲率不同，价格量级也不同。直接比较原参数下的解，无法区分配置变化究竟来自函数形状还是治理价格。为此，以指数成本从基础质量提升至1的增量价格为参照，对其余形式作端点归一化：
\begin{equation}\label{eq:q3_19}
\lambda_f=\frac{g_{\exp}(1)-g_{\exp}(Q_{\mathrm{base}})}{g_f(1)-g_f(Q_{\mathrm{base}})},
\qquad h_f^{\mathrm{norm}}(Q)=\lambda_f[g_f(Q)-g_f(Q_{\mathrm{base}})]_+.
\end{equation}

其中 $f$ 表示成本类型。归一化后，各成本在 $Q=1$ 处具有相同增量价格，但区间内的边际成本仍保留各自形状，从而可以比较不同治理路径对配置的影响。

图~\ref{fig:q3_11}比较原参数与归一化参数下的质量轨迹；55个预算--上下文组合中，有19组的最低损失所对应成本形式发生变化。这说明质量投入同时受价格量级和曲率影响，不能据原参数下的一次排序断言某种函数普遍更优。图~\ref{fig:q3_10}同时给出各成本下的预算--上下文轨迹，供下一节比较注意力开销的影响。
\begin{figure}[H]
\centering
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/result_q3_quality_regimes_halfwidth.pdf}
\caption{质量边界轨迹}
\label{fig:q3_10}
\end{subfigure}\hfill
\begin{subfigure}[b]{.49\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/result_q3_quality_cost_normalized_halfwidth.pdf}
\caption{归一化成本下的质量轨迹}
\label{fig:q3_11}
\end{subfigure}
\caption{不同预算、上下文与成本口径下的质量配置轨迹}
\label{fig:q3_quality_paths}
\end{figure}

\subsubsection{注意力临界长度与上下文敏感性检验}

上下文长度不改变本问的决策维度，而是通过注意力开销改变预算约束。由注意力成本与训练成本之比可直接得到临界长度：
\begin{equation}\label{eq:q3_10}
\frac{C_{\mathrm{attn}}}{C_{\mathrm{train}}}
=\frac{\eta_{\mathrm{attn}}L_{\mathrm{ctx}}}{6},\qquad
L_{\mathrm{ctx}}^{\mathrm{crit}}=\frac{6}{\eta_{\mathrm{attn}}}=30000.
\end{equation}

这一比值与 $N,D$ 和预算均无关。C7的五档长度2048、4096、8192、32768、131072均纳入比较，其中32768和131072已超过临界值，注意力开销大于基础训练开销。

表~\ref{tab:q3_4}固定预算为 $10^{22}$ FLOPs并采用幂成本，比较原25锚点联合解在五档上下文下的变化。随着上下文增大，注意力/训练成本比由 $0.06827$ 增至 $4.36907$，配置中的 $N$ 由 $2.8157$ B降至 $1.1338$ B，$D$ 由 $448.44$ B降至 $245.24$ B，平均损失相应上升。结合图~\ref{fig:q3_10}可见，上下文需求既压缩规模预算，也会影响治理质量达到边界的预算区间。
\begin{table}[htbp]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{原25锚点联合解的上下文配置比较}\label{tab:q3_4}
\begin{tabularx}{\textwidth}{@{}P{2.0cm}C{3.1cm}Y Y Y Y@{}}\toprule
$L_{\mathrm{ctx}}$ & 注意力/训练比 & $N$（B） & $D$（B） & $Q$ & $J$ \\
\midrule
2048 & 0.06827 & 2.8157 & 448.44 & 1.0000 & 1.69176 \\
4096 & 0.13653 & 2.7163 & 439.08 & 1.0000 & 1.69523 \\
8192 & 0.27307 & 2.5438 & 422.26 & 1.0000 & 1.70169 \\
32768 & 1.09227 & 1.9152 & 353.43 & 1.0000 & 1.73153 \\
131072 & 4.36907 & 1.1338 & 245.24 & 1.0000 & 1.79558 \\
\bottomrule\end{tabularx}
\end{table}

\FloatBarrier
\subsection{跨预算资源配置的结构性转移识别}

\subsubsection{基于质量边界与配方支持集的转移识别}

预算增加时，资源数值变化是普遍现象，但平滑的数量增长不等于策略发生质变。本文从质量约束是否激活以及配方主要支持是否改变两个方面表征结构，并要求变化具有持续性。质量状态按 $Q$ 与上下界的距离划分为下界、内点和上界；配方支持集与相邻配置的支持距离定义为
\begin{equation}\label{eq:q3_20}
S_\tau(p)=\{i:p_i>\tau\},\qquad
d_J(S_0,S_1)=1-\frac{|S_0\cap S_1|}{|S_0\cup S_1|},
\qquad \tau=0.01.
\end{equation}

仅有支持集跨阈值还不足以判定转移。本文同时要求 $d_J\ge0.2$ 与 $\|p_1-p_0\|_1\ge0.02$，并检查该变化在后续预算点及加密预算中是否保持。候选事件先由11档预算定位，再以41档预算复核，从而将单点波动与持续变化分开。

质量边界事件还需与式~\eqref{eq:q3_12}的边际方向一致，并在桥接形式、收益强度、随机种子、边界及锚点扰动下接受检验。主判据要求不少于70\%的情景支持同一事件，同时提高门槛检查识别结果的稳定性。图~\ref{fig:q3_10}仅作为联合配置质量轨迹的参照，固定配方事件与联合配置事件分别判断。

\subsubsection{结构转移的多情景检验与阈值敏感性分析}

表~\ref{tab:q3_8}汇总识别结果。固定配方、指数成本且上下文为131072时，出现一次治理质量由下界进入内点的启动事件；预算加密将区间从 $[3\times10^{19},10^{20}]$ 缩小至约 $[7.50\times10^{19},10^{20}]$ FLOPs，粗细扫描对应同一事件。
\begin{table}[H]\centering\setlength{\tabcolsep}{3pt}\renewcommand{\arraystretch}{1.12}
\caption{结构转移识别与阈值敏感性}\label{tab:q3_8}
\begin{tabularx}{\textwidth}{@{}P{3.3cm}C{8.3cm}Y@{}}\toprule
项目 & 实际结果 & 判断 \\
\midrule
固定配方质量启动 & 指数成本、$L_{\mathrm{ctx}}=131072$：下界$\to$内点 & 质量治理由下界进入内点 \\
预算区间（FLOPs） & 粗 $[3\times10^{19},10^{20}]$；细 $[7.50\times10^{19},10^{20}]$ & 两个区间属于同一事件 \\
情景支持比例 & $16/21=76.19\%$ & 通过70\%门槛，未通过80\%门槛 \\
质量方向容差 & $10^{-4}$通过；$10^{-5}$不通过 & 阈值会影响事件识别 \\
联合配置 & 70\%门槛下无稳定事件 & 联合策略未形成稳定结构转移 \\
光滑配方负对照 & 占比均为正，仍跨 $1\%$ 阈值 & 配方支持变化具有连续性 \\
\bottomrule\end{tabularx}
\end{table}

该事件获得16/21，即 $76.19\%$ 的情景支持，通过70\%门槛，但未达到80\%；方向容差由 $10^{-4}$ 收紧至 $10^{-5}$ 后也不再通过。因此，它反映的是特定配置下的局部质量启动，不能推广为所有配置共有的相变。

在联合配置中，未发现通过70\%情景门槛的稳定结构转移。光滑配方负对照还表明，即使所有份额始终为正，连续变化也可能跨过1\%的支持阈值。因此，结构判断必须同时检查变化幅度、持续性和情景一致性，不能仅凭热图颜色突变或某一领域进入支持集作出结论。

\subsection{本问结论}

在统一计入训练、质量治理与注意力成本后，本文将规模、数据量、领域配比和治理质量纳入同一约束优化模型。原25锚点条件下，联合配置较同预算规模基线的平均预测损失改善 $4.1603\%$--$5.8086\%$，但165组中162组至少有一个评测域退步，说明整体收益伴随领域间权衡。全A4方向检查进一步发现初始候选集合的限制，并通过锚点增广获得更低的可行预测损失。

质量治理的绝对投入与成本占比可呈现不同趋势，成本函数的价格尺度和曲率也会改变配置。注意力与训练开销相当的临界上下文长度为30000，C7中的32768和131072已进入注意力成本占优的区间。因而，预算比较应同时固定成本口径和上下文条件，不能只比较参数量或治理质量。

结构检验在固定配方的局部情景中识别出质量启动，但联合配置未形成跨情景稳定的结构转移。有限候选的最大相对后悔值为 $1.5465\%$，双求解器扰动检查通过59/60，支持所报告配置的数值可复核性。后续能力分析可据此使用各预算下的配置及损失输出；实际训练应用仍需在同一治理流水线上联合校准质量评分、治理FLOPs与13域损失。

\endgroup

% =====================================================
% 七、问题四
% =====================================================
\section{问题四：技术演进分解及能力前沿预测}
\setcounter{figure}{0}
\setcounter{table}{0}
\setcounter{equation}{0}
\begingroup
\renewcommand*{\theHfigure}{q4.\arabic{figure}}
\renewcommand*{\theHtable}{q4.\arabic{table}}
\renewcommand*{\theHequation}{q4.\arabic{equation}}
\normalsize
\emergencystretch=2em
\setlength{\parskip}{0pt}
\setlength{\textfloatsep}{5pt plus 1pt minus 1pt}
\setlength{\floatsep}{4pt plus 1pt minus 1pt}
\setlength{\intextsep}{5pt plus 1pt minus 1pt}
\setlength{\abovedisplayskip}{6pt}
\setlength{\belowdisplayskip}{6pt}
\setlength{\belowcaptionskip}{2pt}
\input{q4_body}
\FloatBarrier
\endgroup

% =====================================================
% 八、结论、局限与推广
% =====================================================
\section{结论、局限与推广}

\clearpage

问题四通过来源偏差不变的 Loss--Benchmark 桥接，将前三问最优损失映射到现实能力尺度；在共同支持边界外采用部分识别而非点外推，并在统一状态方程下分析算力放缓时的未来能力前沿及其不确定性来源。该处理将历史贡献分解与情景预测置于同一机制接口之下，同时保留桥接误差、样本依赖和支持域外推带来的不确定性。

\bibliographystyle{gmcm}
\bibliography{reference}

\begin{appendices}
    \section{完整数据审计表}
    % TODO

    \section{关键参数与敏感性结果}
    % TODO

    \section{程序代码}
    % TODO
\end{appendices}



\end{document}
